# Faz 2 (CLIP vs SigLIP benchmark) icin gereken minimum bagimliliklar.
# Kurulum (CPU-only PyTorch icin ozel index sarttir, yoksa CUDA'li ~2GB+ wheel iner):
#   python -m venv .venv
#   .venv\Scripts\activate   (Windows)  /  source .venv/Scripts/activate (bu ortamda)
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-benchmark.txt
#
# Asagidaki surumler, bu ortamda kurulan ve test edilen tam surumlerdir (pip freeze).

torch==2.13.0+cpu
transformers==5.16.1
pillow==12.3.0
numpy==2.2.6

# SiglipProcessor'in tokenizer'i icin transitive olarak gerekli (CLIP icin gerekmiyor).
sentencepiece==0.2.2
protobuf==7.36.0

# Faz 3A: export_onnx.py (CLIP vision encoder -> ONNX, .NET/Lens.Core tarafinda
# kullanilir) ve onun tek seferlik dogrulamasi icin. Lens Desktop runtime'inin
# bir parcasi degildir.
onnx==1.17.0
onnxruntime==1.20.1
