# Kararlar — Lens

Bu doküman alınmış ve henüz alınmamış mimari/teknik kararları ayrı ayrı listeler.
Alınmamış kararlarda kesin hüküm verilmez; alternatifler `ARCHITECTURE_PROPOSAL.md`
içinde tartışılır.

---

## Confirmed (Alınmış Kararlar)

> **2026-09-04 kaynak kontrol noktası:** `d4866f1` sonrasında kullanıcının
> açık talimatıyla #75-77'nin kaynak değişiklikleri de commit kapsamına
> alındı. Bu kararlardaki "kaynak commit'i bekliyor / yalnızca doküman"
> ifadeleri önceki kontrol noktasına aittir. Bu kaynakta Release tekrar
> 0 warning / 0 error, hardeningtest 158 PASS / 0 FAIL. Debug/görsel kabul,
> ertelenen #75 sorunları ve publish hâlâ bekliyor. Detay: `CHANGELOG.md`
> ve `docs/PROJECT_CONTEXT.md` son durum kaydı.

| # | Karar | Not |
|---|-------|-----|
| 1 | Proje klasörü git ile versiyonlanacak | `git init` proje başlangıcında yapıldı |
| 2 | İlk PoC kapsamı yalnızca görselle arama | Text search, login vb. dahil değil |
| 3 | İlk demo, klasördeki mevcut görsellerle çalışabilir | Gerçek DB entegrasyonu ilk demo için zorunlu değil |
| 4 | Hedef platform Windows masaüstü | Fabrika içi kullanım |
| 5 | Model karşılaştırması varsayıma değil ölçüme dayanacak | CLIP/SigLIP arasında moda/yenilik gerekçesiyle seçim yapılmayacak |
| 6 | **[Faz 1]** PoC mimarisi local-first | Dış AI servisi kullanılmayacak (PoC kapsamında) |
| 7 | **[Faz 1]** Uygulama dili Python | — |
| 8 | **[Faz 1]** Görsel kaynağı klasör tabanlı | Gerçek DB entegrasyonu sonraki faz |
| 9 | **[Faz 1]** Pipeline: görsel → embedding → cosine similarity → Top-5 sonuç | Önceki "5-10 aday" ifadesi Top-5 olarak netleşti. **⚠️ SUPERSEDED — bkz. #35 (Top-10)** |
| 10 | **[Faz 1]** Nihai kararı kullanıcı verir | Sistem kesin eşleşme iddia etmez |
| 11 | **[Faz 1]** ~1.000 görsel ölçeğinde vector database kullanılmayacak, brute-force arama yapılacak | ARCHITECTURE_PROPOSAL.md Bölüm 10'daki tahmin onaylandı; CPU'da ayrıca ölçülecek. **⚠️ Ölçek varsayımı SUPERSEDED — bkz. #32 (~5000)**. Brute-force kararı kendisi geçerliliğini koruyor, ~5000'de yeniden ölçülecek |
| 12 | **[Faz 1]** GUI bu fazda yok | Sonraki faz konusu |
| 13 | **[Faz 1]** Gerçek fabrika DB entegrasyonu sonraki faz | Değişmedi |
| 14 | **[Faz 2]** CLIP vs SigLIP benchmark tamamlandı (11 görsel + sentetik varyasyon, 55 sorgu) | Top-1: CLIP %98, SigLIP %100. Top-3/Top-5: ikisi de %100. CPU sorgu süresi ~91ms, ikisinde de eşit. Detay: `benchmark/results/report.md`. **Final model seçimi henüz Confirmed değil** — bkz. Not Yet Decided #1 |
| 15 | **[Faz 3]** Ürün, Windows masaüstü uygulaması olarak C#/.NET ile geliştirilecek | Yönetici onayı (2026-09-01). Faz 1/2 Python kodu **silinmeyecek**; benchmark/model değerlendirme aracı olarak kalır, runtime bu değildir |
| 16 | **[Faz 3]** Production hedefinde kullanıcı girişi (login) ve yetkilendirme olacak; **bugünkü MVP'de YOK** | Yönetici onayı (2026-09-01) |
| 17 | **[Faz 3]** Production hedefinde görsel kaynağı iki seçenekli olacak: fabrika DB veya local klasör; **bugünkü MVP'de yalnızca local klasör** | Yönetici onayı (2026-09-01). Faz 1 karar #8 ile tutarlı. **⚠️ SUPERSEDED (2026-09-01, ikinci görüşme) — bkz. #31.** DB'den doğrudan okuma production hedefinden tamamen çıkarıldı; kaynak yalnızca dosya dizini |
| 18 | **[Faz 3]** Production hedefinde yeni ürün girişi Lens uygulaması üzerinden yapılacak; **bugünkü MVP'de ürün ekleme ekranı / CRUD / DB'ye yazma YOK** | Yönetici onayı (2026-09-01). **⚠️ SUPERSEDED (2026-09-01, ikinci görüşme) — bkz. #30.** Yeni ürün ekleme artık kalıcı olarak Lens'in kapsamı dışında, "bugün yok" değil "hiçbir zaman Lens'in sorumluluğu değil" |
| 19 | **[Faz 3]** Text search ileride olabilir; **bugünkü MVP'de YOK** | Yönetici onayı (2026-09-01), Faz 1 karar #2 ile tutarlı |
| 20 | **[Faz 3A]** MVP model: **CLIP** (openai/clip-vit-base-patch16) | Tech Lead/CTO onayı (2026-09-01). **Provisional/reversible karar** — production için kesin model kararı değildir. Gerekçe: Top-5 zaten CLIP=%100/SigLIP=%100 eşit; SigLIP yalnızca `downscale_upscale` varyasyonunda Top-1 avantajı gösterdi; CLIP'in ONNX/.NET entegrasyon riski daha düşük. SigLIP sonucu ve yeniden değerlendirme ihtimali dokümantasyonda kalır (bkz. Confirmed #14) |
| 21 | **[Faz 3A]** WPF (.NET 8) masaüstü UI framework olarak onaylandı | Tech Lead/CTO onayı (2026-09-01) |
| 22 | **[Faz 3A]** Lens.exe içinde Python runtime/process YOK; AI inference ONNX Runtime (`Microsoft.ML.OnnxRuntime`) ile .NET içinde çalışır | Tech Lead/CTO onayı (2026-09-01). Python benchmark kodu ayrı bir engineering aracı olarak kalır (karar #15) |
| 23 | **[Faz 3A]** Embedding'ler kalıcı local cache/index olarak saklanır; her klasör seçiminde yeniden hesaplanmaz | Tech Lead/CTO onayı (2026-09-01) — önceki "kalıcı cache yok" önerisi **reddedildi**. Incremental kontrol: relative path + file size + LastWriteTimeUtc + embedding. Vector DB / SQLite kullanılmayacak (gerçekten gerekmedikçe) |
| 24 | **[Faz 3A]** İlk ürün index'i: 11 gerçek nevresim görseli, .NET/CLIP embedder ile üretilir | Tech Lead/CTO onayı (2026-09-01). Faz 2'nin 55 sentetik varyasyonu ürün index'ine girmez — yalnızca benchmark/test verisidir. Python ve .NET arasında preprocessing tutarsızlığı olmaması için embedding'ler Python'dan aktarılmaz, .NET tarafında yeniden üretilir |
| 25 | **[Faz 3A]** MVP iş kuralı: aynı desenin farklı renk varyasyonu aynı ürün kabul edilir | Tech Lead/CTO onayı (2026-09-01). **Geçici/reversible karar** — ileride değişebilir. Bu varsayım için ekstra grayscale/özel algoritma eklenmeyecek; gerçek sonuçlar gözlemlenecek |
| 26 | **[Faz 3A]** Query görselleri MVP'de temiz/katalog tipi olacak | Tech Lead/CTO onayı (2026-09-01). Karmaşık telefon fotoğrafı, kırışık kumaş, perspektif farkı, karmaşık arka plan MVP kapsamı dışında |
| 27 | **[Faz 3A]** Final MVP dağıtımı: self-contained Windows publish (Visual Studio/.NET SDK/Python gerekmeden çalışır) | Tech Lead/CTO onayı (2026-09-01). Yönetici bilgisayarı donanım açısından yeterli kabul edilir, ek optimizasyon yapılmayacak |
| 28 | **[Faz 3A]** Gerçek ürün görselleri (`nevresim/`) ve büyük ONNX model dosyaları GitHub'a commit edilmeyecek | Tech Lead/CTO onayı (2026-09-01). `.gitignore` koruması sürdürülür. Repo public/private durumu ve model dağıtım yaklaşımı ayrı değerlendirilecek |
| 29 | **[Faz 3A]** Çalışma sırası fazlara bölündü: önce minimal .NET AI proof (GUI'siz/minimal), sonra onay sonrası Faz 3B (tam WPF UI) | Tech Lead/CTO onayı (2026-09-01) |
| 30 | **[Faz 4]** Yeni ürün/desen ekleme Lens'in sorumluluğu değil; başka bir sistem/süreç tarafından ürün dizinine eklenir | Yönetici görüşmesi (2026-09-01, ikinci görüşme). **SUPERSEDES #18.** Detay: `docs/PRODUCTION_REQUIREMENTS.md` §1 |
| 31 | **[Faz 4]** Görsel kaynağı yalnızca **dosya dizini**; DB'den doğrudan okuma yok | Yönetici görüşmesi (2026-09-01). **SUPERSEDES #17.** Detay: `docs/PRODUCTION_REQUIREMENTS.md` §2 |
| 32 | **[Faz 4]** Gerçek ölçek: **~5000 görsel**, **tek (flat) klasör**; recursive alt klasör taraması şu an gerekli değil | Yönetici görüşmesi (2026-09-01). **SUPERSEDES** #11'deki ~1.000 ölçek varsayımını. Detay: `docs/PRODUCTION_REQUIREMENTS.md` §2 |
| 33 | **[Faz 4]** Varsayılan ürün dizini **configurable** olacak (hardcode edilmeyecek); **UNC network path** birincil yaklaşım | Yönetici görüşmesi (2026-09-01). Gerçek path henüz bilinmiyor — blocker değil. Detay: `docs/PRODUCTION_REQUIREMENTS.md` §3 |
| 34 | **[Faz 4]** Varsayılan dizine erişilemezse uygulama **crash olmayacak**; kullanıcı dostu durum mesajı + manuel klasör seçme sunulacak | Yönetici görüşmesi (2026-09-01) |
| 35 | **[Faz 4]** **Top-10** gösterilecek (Top-5 yerine); arama algoritması değişmiyor | Yönetici görüşmesi (2026-09-01). **SUPERSEDES #9**. **⚠️ SUPERSEDED — bkz. #60 (minimum benzerlik eşiği + en fazla 15 sonuç)** |
| 36 | **[Faz 4]** Logging confirmed requirement: persistent log dosyası + UI'da indeksleme özeti | Yönetici görüşmesi (2026-09-01). Detay: `docs/PRODUCTION_REQUIREMENTS.md` §8 |
| 37 | **[Faz 4]** Dosya sınıflandırması: yalnızca `.jpg/.jpeg/.png` image pipeline'a girer; diğer dosyalar (`.json`, `.txt` vb.) hata sayılmadan atlanır; bilinen ama desteklenmeyen görsel formatları (`.tif`, `.bmp`, `.webp`) ayrı "unsupported format" olarak sınıflandırılır | Yönetici görüşmesi (2026-09-01). Detay: `docs/PRODUCTION_REQUIREMENTS.md` §7 |
| 38 | **[Faz 4]** UI'da sınırlı görsel iyileştirme (hiyerarşi, accent color, durum renkleri); büyük redesign/animasyon/ağır tema YOK | Yönetici görüşmesi (2026-09-01) |
| 39 | **[Faz 4A]** Ürün dizini (paylaşılan UNC klasör) yalnızca görsel içerir; her kullanıcı/PC kendi local index/cache'ini `%LocalAppData%\Lens\` altında tutar | **Confirmed** — Yönetici onayı (2026-09-01, FAZ 4A kickoff). Önceden Tech Lead önerisiydi (Recommended), şimdi onaylandı. Detay: `docs/PRODUCTION_REQUIREMENTS.md` §5. **⚠️ SUPERSEDED — bkz. #61 (shared index artık ürün dizininin kendi içinde, `.lens/index.json`)** |
| 40 | **[Faz 4A]** Index dosyası yazımı **atomic** olacak (temp dosya + replace) | **Confirmed** — Yönetici onayı (2026-09-01). ~5000 ölçekte tam-dosya-yeniden-yazma sırasında kesinti/çökme durumunda index kaybı riskini azaltmak için |
| 41 | **[Faz 4A]** Varsayılan dizin, exe yanında duran basit bir JSON config dosyasından (`appsettings.json`, kendi `System.Text.Json` kodumuzla, **ek NuGet yok**) okunacak; klasik `Properties.Settings`/`user.config` kullanılmayacak | **Confirmed** — Yönetici onayı (2026-09-01). Gerekçe: `user.config` sürüm-bazlı path taşınma sorunu |
| 42 | **[Faz 4A]** Admin default ve user override ayrı tutulur: `AdminDefaultProductDirectory` (salt-okunur, exe yanındaki config'te) vs `UserOverrideProductDirectory` + `UseUserOverride` (yazılabilir, `%LocalAppData%\Lens\config\` altında) | Yönetici onayı (2026-09-01) |
| 43 | **[Faz 4A]** Kullanıcının seçtiği alternatif klasör **varsayılan olarak geçici**dir (session-only, restart'ta admin default'a döner); kullanıcı açıkça "Bu klasörü varsayılan olarak kullan" derse `UseUserOverride=true` yazılıp kalıcı olur | Yönetici onayı (2026-09-01) |
| 44 | **[Faz 4A]** Cache anahtarı: ürün dizini path'inden türetilen deterministic bir tanımlayıcı (hash); farklı ürün dizinleri farklı cache klasörü kullanır, aynı dizine dönülünce eski cache tekrar kullanılır | Yönetici onayı (2026-09-01) |
| 45 | **[Faz 4A]** `%LocalAppData%\Lens\` altında `config/`, `cache/`, `logs/` alt klasör yapısı; `logs/` bu fazda oluşturulur ama kullanılmaz (logging implementasyonu FAZ 4C) | Yönetici onayı (2026-09-01) |
| 46 | **[Faz 4B]** Search-before-refresh: "Ara" öncesi ürün dizini freshness kontrolü yapılır; yeni/değişmiş/silinmiş dosya varsa arama öncesi incremental update tetiklenir; kısa süre önce güncellenmişse tekrar taranmaz | Yönetici onayı (2026-09-01). **Implement edildi ve kabul edildi** (FAZ 4B, 2026-09-01). Freshness TTL = **30 saniye** (yönetici düzeltmesi). Detay: `docs/ROADMAP.md` FAZ 4B |
| 49 | **[Faz 4B]** Dosya sınıflandırması `SupportedImage` / `UnsupportedImageFormat` / `NonImage` olarak 3 kategoride uygulandı; `IndexUpdateStats`/`IndexFileIssue`/`ChangeSummary` veri modeli eklendi; toplam tarama hatasında önceki geçerli index korunur | Yönetici onayı (2026-09-01, FAZ 4B kabul). Detay: `docs/ROADMAP.md` FAZ 4B |
| 50 | **[Faz 4C]** Logging: kendi kodu ile `ILensLogger`/`FileLogger` (ek NuGet yok); düşük coupling gereği `ImageIndex` loglamadan habersiz bırakıldı, log satırları çağıran katmanın (WPF) döndürdüğü istatistiklerden üretiliyor. Format: `%LocalAppData%\Lens\logs\lens-yyyyMMdd.log`, düz UTF-8, `[INFO/WARNING/ERROR]`, 30 günlük retention, loglama hatası uygulamayı çökertmez | Yönetici onayı (2026-09-01, FAZ 4C kabul). **Resolves Not Yet Decided #7.** Detay: `docs/ROADMAP.md` FAZ 4C |
| 51 | **[Faz 4C]** Ana ekrandaki indeksleme özeti sadeleştirildi: yalnızca kullanıcıya anlamlı bilgi gösterilir (sıfır olan sayaçlar/"değişmeyen" ana özette gösterilmez); ayrıntılı sayaçlar yalnızca log dosyasında ve "Sorunlu Dosyalar" penceresinde kalır | Yönetici onayı (2026-09-01, FAZ 4C sırasında). Veri modeli (IndexUpdateStats) değişmedi, yalnızca UI metni sadeleştirildi |
| 47 | **[Faz 4D]** Query görseli için WPF native drag & drop desteği (mevcut "Sorgu Görseli Seç" butonuna ek, yeni dependency yok) | Yönetici onayı (2026-09-01). **Implement edildi ve kabul edildi** (FAZ 4D, 2026-09-01). Detay: `docs/ROADMAP.md` FAZ 4D |
| 48 | **[Faz 4D]** Sonuç ekranı: query görseli üstte büyük/sürekli görünür; Top-10 altta sürekli görünür grid; Top-10'dan tıklanan sonuç ayrı bir "karşılaştırma" alanında büyük gösterilir + similarity score (query değişmez, sadece karşılaştırma alanı güncellenir) | Yönetici onayı (2026-09-01). **Implement edildi ve kabul edildi** (FAZ 4D, 2026-09-01). Detay: `docs/ROADMAP.md` FAZ 4D |
| 52 | **[Faz 4D]** "Yeni Arama" butonu (query/karşılaştırma/Top-10 temizlenir, ürün klasörü/index/cache korunur); minimal "⋮" menü (Ayarlar — salt-okunur özet, Log Klasörünü Aç, Hakkında); Top-10/query/karşılaştırma görselleri için çift-tıkla büyük önizleme/zoom penceresi (`ImagePreviewWindow`: fare tekerleği zoom, sürükle-pan, çift tık sığdır, ESC kapat, aynı anda tek önizleme açık) | Yönetici onayı (2026-09-01, FAZ 4D kabul + son UI/UX polish turu). Detay: `docs/ROADMAP.md` FAZ 4D |
| 53 | **[Faz 4D]** Önizleme penceresi boyutu görselin tam piksel çözünürlüğüne göre değil, ana ekrandaki küçük önizleme kutusuna (220px) göre "biraz daha büyük" (uzun kenar ~480px + pay) hesaplanır; ekran çalışma alanının %85'i ile üst sınırlanır | Yönetici onayı (2026-09-01) — ilk versiyon (görselin tam çözünürlüğüne göre boyutlandırma) ekranı gereğinden fazla kapladığı için düzeltildi |
| 55 | **[Faz 4E]** Geçici dosya/network hatası ile gerçek silme farklı ele alınır: bir dosyanın metadata okuma/embed adımı geçici bir nedenle (network kesintisi, dosya kilidi, izin sorunu) başarısız olursa ve dosya directory taramasında hâlâ görünüyorsa, önceki sağlam kaydı sonuca aynen taşınır (removed sayılmaz); yalnızca gerçekten görünmeyen dosyalar silinmiş sayılır | Codex production audit sonrası kritik düzeltme (2026-09-02). Detay: `docs/ROADMAP.md` FAZ 4E |
| 56 | **[Faz 4E]** Bozuk/uyumsuz cache dosyası (yarım JSON, null/eksik alan, embedding boyutu ≠512, NaN/Infinity) "hepsi ya da hiçbiri" politikasıyla tamamen güvensiz sayılır: uygulama çökmez, cache yok sayılıp normal "index yok, yeniden oluştur" akışına düşülür; kısmi kurtarma/migration bu fazda yapılmaz | Codex production audit sonrası kritik düzeltme (2026-09-02). Detay: `docs/ROADMAP.md` FAZ 4E |
| 57 | **[Faz 4E]** Açılıştaki varsayılan dizin çözümlemesi ve "İndeksi Güncelle" ön-kontrolü UI thread'ini bloklamaz (`Task.Run`); UNC/network erişim hatası kullanıcı dostu duruma çevrilir, busy state her çıkış yolunda sıfırlanır | Codex production audit sonrası kritik düzeltme (2026-09-02). Detay: `docs/ROADMAP.md` FAZ 4E |
| 58 | **[Faz 4E]** Büyük/aşırı çözünürlüklü görsel resource guard: tek kontrol noktasından (~50 MB dosya, ~50 MP piksel, `Image.Identify` ile tam decode öncesi) indexing + query embedding + büyük önizleme/zoom'un üçünde de uygulanır; limiti aşan ürün görseli embed edilmeden Issue olarak kaydedilir (eski sağlam kaydı korunur), query tarafında arama başlamadan reddedilir | Codex production audit sonrası eklenen 4. kritik madde (2026-09-02). Yeni dependency eklenmedi (mevcut ImageSharp). Detay: `docs/ROADMAP.md` FAZ 4E. **⚠️ SUPERSEDED — bkz. #64 (hard MB/MP reddi tamamen kaldırıldı, ekonomik decode ile değiştirildi)** |
| 59 | **[Faz 4E]** Görsel olmayan/desteklenmeyen dosyalar (`.pdf`, `.zip`, `.txt`, `.csv` vb.) artık sessizce yok sayılmaz; `FileIssueKind.NonImageFile` olarak Issues'a "Desteklenmeyen dosya türü" etiketiyle eklenir (decode denenmez, indeksleme durmaz). Yalnızca Lens'in kendi eski artefaktı (`.lens_index.json`) ve Windows sistem dosyaları (`Thumbs.db`, `desktop.ini`) tamamen görünmez kalır | Kullanıcı geri bildirimi (2026-09-02). SUPERSEDES eski "NON-IMAGE/SKIPPED = sessiz" varsayımı (bkz. PRODUCTION_REQUIREMENTS.md §7) |
| 54 | **[Faz 4D]** Son görsel geri bildirim turu: sürükle-bırak sırasında QueryDropZone'da accent border/hafif arka plan/"Görseli buraya bırak" metni (DragLeave/Drop'ta normale döner, geçersiz dosyada gösterilmez); karşılaştırma alanında görüntülenen benzerlik değeri tam **%100.0** ise yeşil vurgu (biçimlendirilmiş metin karşılaştırması, ham `double == 1.0` değil); query paneli border'ı her zaman accent renkli/2px (sonuç paneli nötr kalır) — "arattığım görsel bu" hissi. Query, karşılaştırma sonucu ve Top-10 kartlarındaki dosya adları salt-okunur ama seçilebilir/kopyalanabilir metin kutusu (I-beam imleç, Ctrl+C, sağ-tık kopyala); Top-10 kartlarında dosya adı butonun DIŞINA alındı ki metin seçimi kart tıklama/seçim davranışını bozmasın. Sürükleme sırasında küçük, yarı saydam bir thumbnail (native WPF `Adorner`, `IsHitTestVisible=false`) fareyi takip eder — Explorer'ın kendi "ghost" görseli Lens penceresine girince kaybolduğu için eklendi | Yönetici onayı (2026-09-01/02, canlı kullanım geri bildirimi). Gerçek OS-seviyesi sürükleme davranışı (drag preview'ın fareyi takip etmesi) otomasyonla test edilemedi, manuel doğrulama gerekiyor |
| 60 | **[Faz 1 — manager requirement paketi]** Sabit Top-N kaldırıldı: kullanıcı arama öncesi **minimum benzerlik (%) eşiği** girer (`score >= threshold`, inclusive, 0-100, ondalık/TR virgül destekli); eşiği geçen sonuçlar azalan sırada **en fazla 15** olarak gösterilir | Product owner talimatı (2026-09-03). **SUPERSEDES #35 (Top-10) ve #9 (Top-5)**. Detay: `docs/PRODUCTION_REQUIREMENTS.md` §Arama |
| 61 | **[Faz 1]** Canonical (paylaşılan) index artık `%LocalAppData%` DEĞİL, ürün dizininin kendi içinde: `<ProductDirectory>/.lens/index.json` (UNC olabilir); eski LocalAppData product index normal operasyonda okunmuyor/yazılmıyor ama otomatik silinmiyor de | Product owner talimatı (2026-09-03). **SUPERSEDES #39, #44, #45'in "cache" kısmı** (config/logs hâlâ LocalAppData'da). Detay: `docs/ARCHITECTURE.md`, `docs/PRODUCTION_REQUIREMENTS.md` §5 |
| 62 | **[Faz 1]** Shared index için tek-yazarlı (single-writer) exclusive dosya kilidi zorunlu: `<ProductDirectory>/.lens/index.lock` üzerinde `FileShare.None` ile alınan bir OS handle; writer load→scan→update→save boyunca tutar, reader kilit almaz (stable `index.json`'u her zaman okuyabilir). Distributed lock/queue/servis eklenmedi | Product owner talimatı (2026-09-03). Detay: `Lens.Core.Indexing.IndexLock`, `docs/PRODUCTION_REQUIREMENTS.md` §Concurrency |
| 63 | **[Faz 1]** Atomic save UNC/paylaşılan klasör için güçlendirildi: benzersiz (GUID) temp dosya adı, hedef önceden silinmez, `File.Replace` başarısız olursa (bazı ağ dosya sistemleri) `File.Move(overwrite:true)`'a düşer | Product owner talimatı (2026-09-03). Gerçek SMB share üzerinde manuel acceptance gerekiyor (bkz. final rapor) |
| 64 | **[Faz 1]** Sabit 50MB dosya boyutu / 50MP çözünürlük reddi tamamen kaldırıldı; geçerli bir fabrika deseni artık SADECE büyük olduğu için reddedilmez. Aynı eşik değeri (50MP), reddetme amacıyla değil, ImageSharp `DecoderOptions.TargetSize` ile ekonomik (downsampled) decode tetikleme "ipucu" olarak yeniden kullanıldı — yeni bir sabit reddetme eşiği EKLENMEDİ | Product owner talimatı (2026-09-03) — kesin karar. **SUPERSEDES #58**. Detay: `Lens.Core.Ai.ImageResourceLimits`, `Lens.Core.Ai.ImagePreprocessor.LoadForPreprocessing` |
| 65 | **[Faz 1]** "Arama öncesi indeksi otomatik kontrol et ve güncelle" checkbox eklendi; varsayılan **açık**, tercih `user-settings.json`'da kalıcı (eski dosyada alan yoksa geriye uyumlu açık kabul edilir). Kapalıyken "Ara" hiçbir scan/write yapmaz, yalnızca bellekteki mevcut stable index'i kullanır; manuel "İndeksi Güncelle" butonu checkbox'tan bağımsız her zaman force scan yapar | Product owner talimatı (2026-09-03) — kesin karar (default açık). Detay: `Lens.Core.Config.UserSettings.AutoIndexBeforeSearch` |
| 66 | **[Görsel güncelleme]** Ana pencere zemini `#F5F6F8`'den koyu slate `#FF64748B`'ye değiştirildi; amaç beyaz/açık renkli ürün desenlerinin zeminden belirgin ayrışması. Sonuç/desen kartları ve sorgu/seçilen-sonuç görsel kutuları saf beyaz (`#FFFFFF`) korundu; üst kontrol/durum/sayaç ve sorgu-eşik çubuğu (Satır 0-3) ile alt bilgi metni okunabilirlik için açık renkli tek bir panel (`LightPanelBrush`, `#F5F6F8`) üzerine alındı; koyu zeminde kalan geri kalan metinler (karşılaştırma başlıkları/dosya adları/ipucu, sonuç listesi başlığı, %100 eşleşme vurgusu) için yeni `OnDarkTextBrush`/`OnDarkSecondaryTextBrush`/`OnDarkSuccessBrush` kaynakları eklendi. Sorgu görselinin nötr çerçevesi ve mavi seçili/vurgu renkleri değişmedi; arama/threshold/indeksleme/kilit mantığına dokunulmadı | Kullanıcı talimatı (2026-09-04) — yalnızca `MainWindow.xaml` ve `MainWindow.xaml.cs` (3 satırlık Foreground ataması). Detay: `CHANGELOG.md` "Görsel Güncelleme — Zemin Rengi Kontrastı" **⚠️ SUPERSEDED — bkz. #67 (tek sabit zemin/panel yerine 6 seçilebilir tema + DynamicResource mimarisi)** |
| 67 | **[Görsel güncelleme - tema turu]** Sabit tek zemin (#66) yerine kullanıcının menüden seçebileceği 6 tema: Açık (`#F5F6F8`), Normal (`#64748B`, varsayılan), Koyu (`#334155`), Açık Sepya (`#E8DCC8`), Koyu Sepya (`#705C46`), Lime — deneysel (`#D4E157`). Önceki turun sabit açık panel blokları (üst kontrol şeridi, sonuç viewport arka planı, alt bilgi) kaldırıldı — bu alanlar artık doğrudan seçili temanın zeminiyle bütünleşir; kart/thumbnail/sorgu-sonuç görsel kutuları her temada nötr beyaz kalır. Renk kaynakları (`NeutralTextBrush`, `SectionHeaderBrush`, `SecondaryTextBrush`, `SuccessBrush`, `WarningBrush`, `MainBackgroundBrush`) `DynamicResource` ile tema değişiminde canlı güncellenir (Resources[key] = yeni SolidColorBrush); `AccentBrush`/`AccentBrushLight`/`NeutralBorderBrush` temadan bağımsız sabit kalır (mavi seçim vurgusu ve sorgu panelinin nötr/koyu çerçevesi her temada aynı). `ProblemFilesButton` gibi native (temadan etkilenmeyen açık gri) buton yüzeyi üzerindeki kırmızı metin BİLEREK sabit/hardcoded bırakıldı (WarningBrush artık tema-bağımlı olduğu için doğrudan kullanılamaz) | Kullanıcı talimatı (2026-09-04). Yeni framework/kapsamlı ayarlar ekranı eklenmedi - mevcut ⋮ menüsüne sade bir "Arka Plan" alt menüsü. Detay: `Lens.Desktop.AppTheme`/`ThemePalette`, `MainWindow.SetTheme`, `CHANGELOG.md` |
| 68 | **[Görsel güncelleme - tema turu]** Tema tercihi `UserSettings.Theme` (Lens.Core, varsayılan `"Normal"`) alanında, kullanıcının kendi `%LocalAppData%\Lens\config\user-settings.json` dosyasında saklanır — shared index/ürün klasörüne YAZILMAZ, başka bir kullanıcıyı/bilgisayarı etkilemez. Bilinmeyen/geçersiz/eski (alanı içermeyen) değer `MainWindow.ParseTheme` ile güvenle "Normal"e döner. Kayıt Load→değiştir→Save akışıyla yapılır ki `AutoIndexBeforeSearch` ve kullanıcı klasörü override'ı gibi diğer alanlar kaybolmasın. Açılışta kayıtlı tema SADECE uygulanır (`persist:false`) - diske tekrar YAZILMAZ, böylece "açılış tema yükleme olayı yanlışlıkla varsayılanı kaydedip mevcut tercihi ezer" riski yoktur | Kullanıcı talimatı (2026-09-04). Detay: `Lens.Core.Config.UserSettings.Theme`, `Lens.AiProof` Grup I testleri |
| 69 | **[Görsel güncelleme - kart/kaydırma düzeltmesi]** İki bağımsız düzeltme aynı turda yapıldı: (1) Sonuç kartlarının içerik-kadar-yükseklik olması: `ResultsItemsControl`'e `VerticalAlignment="Top"` eklendi - kök neden `UniformGrid`'in kendisine verilen ARRANGE boyutunu (ScrollViewer'ın boş kalan tüm yüksekliği) satır sayısına bölerek hücre yüksekliğini hesaplaması, az sonuçta (ör. 1-2 kart) seçili kartın mavi çerçevesini/arka planını gereksiz yere aşağı uzatıyordu. Mevcut bounded ScrollViewer (Auto başlık + `*` viewport) sözleşmesi korundu, 5 sütunlu yatay düzen değişmedi. (2) Sonuç viewport'u (artık adlandırılmış `ResultsScrollViewer`) yeni arama/yeni sorgu görseli/"Yeni Arama"/ürün klasörü değişiminde `Dispatcher.Loaded` önceliğiyle TEK SEFERLİK `ScrollToTop()` ile başa döndürülür - eski kaydırma konumu bir sonraki aramaya taşınmaz; kart seçimi/önizleme/tema değişimi/pencere yeniden boyutlandırma bu resetten ETKİLENMEZ | Kullanıcı talimatı (2026-09-04) - Codex'in salt-okunur bulgusu üzerine. Similarity hesaplaması, threshold sözleşmesi, maksimum 15 sonuç, indeksleme/kilit mantığına dokunulmadı. Detay: `CHANGELOG.md`, `MainWindow.ResetResultsScroll` |
| 70 | **[Görsel güncelleme]** Sorgu ve seçilen sonuç görsel çifti, iki bağımsız `*` sütununda ayrı ayrı ortalanmak yerine TEK bir grup olarak (`Auto` + 40 DIP sabit boşluk + `Auto` sütunlu iç `Grid`, dış `Grid` `HorizontalAlignment="Center"`) pencerede birlikte ortalanır; pencere genişledikçe aradaki mesafe sabit kalır, fazla alan grubun iki yanına eşit dağılır. 220×220 görsel kutuları, mavi vurgu ve nötr çerçeve değişmedi | Kullanıcı talimatı (2026-09-04). Detay: `CHANGELOG.md` |
| 71 | **[Düzeltme - eski sonuçların erken temizlenmesi]** `SearchButton_Click`'te önceki davranış: eski sonuç kartları/karşılaştırma/kaydırma yalnızca YENİ arama BAŞARIYLA tamamlandıktan sonra temizleniyordu; index hazırlığı (freshness kontrolü/otomatik indeksleme) veya embed sırasında bir hata/uzun gecikme olursa, önceki aramaya ait sonuçlar ekranda öylece kalıp yeni sorgunun sonucuymuş gibi görünebiliyordu (#69'un scroll-reset düzeltmesi bu senaryoyu kapsamıyordu, çünkü `ResetResultsScroll`/`_results.Clear()` hâlâ arama SONUNDA çağrılıyordu). Düzeltme: girdi validasyonu (klasör/görsel/threshold) BAŞARILI olur olmaz -- index hazırlığı/model yükleme dahil hiçbir uzun süren işlem başlamadan ÖNCE -- eski sonuçlar/karşılaştırma/kaydırma temizlenir; sorgu görseli, threshold girdisi, tema ve kullanıcı ayarları bundan etkilenmez. Geçersiz/eksik girdide (erken `return`) bu temizleme koduna hiç ulaşılmadığından mevcut ekran/kaydırma AYNEN korunur. Ardından model yüklenemezse veya arama sırasında istisna oluşursa artık zaten temizlenmiş ekranda açıklayıcı bir durum mesajı (`SetIndexStatus`) gösterilir - eski sonuçlar bir daha asla yeni sorgunun sonucu gibi görünmez | Kullanıcı talimatı (2026-09-04) - küçük düzeltme + gerçek UI Automation ile doğrulama turu. Similarity/threshold/indeksleme/kilit mantığına dokunulmadı. Doğrulama: aynı görsel+farklı eşikle kaydırma sıfırlama (gerçekten kaydırma gerektiren 15 sonuçla, `ScrollPercent=-1` tuzağına düşülmeden), sorgu dosyası silinerek üretilen gerçek bir hata sonrası eski sonuç/karşılaştırmanın kalmadığı, geçersiz eşikte ekranın/kaydırmanın AYNEN korunduğu, tema değişimi/görünür bir karta tıklamanın kaydırmayı bozmadığı ve dosya adı metninin gerçek Ctrl+C ile panoya kopyalandığı UI Automation (System.Windows.Automation) ile canlı çalışan uygulama üzerinde test edildi (bkz. faz raporu). Detay: `CHANGELOG.md`, `MainWindow.SearchButton_Click` |
| 72 | **[Sonuç sınırı 15 → 200, temiz teslim paketi]** Önceki "kullanıcıdan sonuç sayısı al / 1-100 seç" açık sorusunun YERİNE bu karar geçti: kullanıcıya yeni bir giriş alanı EKLENMEDİ, sabit `SimilaritySearch.MaxResults` 15'ten 200'e çıkarıldı. Mevcut sözleşme aynen korunuyor: minimum benzerlik eşiğini karşılayan sonuçlar skora göre azalan sırada getirilir, 200'den fazlaysa yalnızca en iyi 200'ü gösterilir (fazlası atılır), 200'den azsa DOLDURMA/PADDING yapılmaz (benzerlik hesaplaması değişmedi). UI: "EN BENZER SONUÇLAR (EN FAZLA 15)" başlığı → "EN BENZER SONUÇLAR" (arayüzde sabit üst sınır sayısı BİLEREK gösterilmiyor); durum mesajı "N sonuç bulundu." → "N sonuç gösteriliyor." (N = ekranda gerçekten görünen kart sayısı, kesilmiş listede toplam eşleşme sayısı gibi sunulmuyor). Tema/kart boyutu/ortalanmış karşılaştırma/kaydırma düzeni ve #71'in "eski sonuçları arama başlar başlamaz temizle" düzeltmesi değişmeden korundu. **Performans:** 200 sonuca kadar thumbnail decode'u (`TryLoadPreview`), önceden UI thread'de arama sonrası sırayla yapılırken artık mevcut arka plan `Task.Run`'ına taşındı (BitmapImage zaten Freeze() ediliyordu, thread-safe taşınabiliyor) - sabit bir UI donma riskini gidermek için BİLEREK sıralı bırakıldı, sınırsız paralel decode eklenmedi. **Test:** `Lens.AiProof hardeningtest` Grup F, 0/1/6/15/200/250 eşleşme senaryolarını (azalan sıra + en iyi 200 seçimi + doldurma olmadığı dahil) ayrı ayrı kapsayacak şekilde güncellendi, 78/78 PASS. **Temiz teslim paketi:** `publish/Lens.Desktop-win-x64-themes/`, `dotnet clean` sonrası `dotnet publish -p:DebugType=None` ile yeniden üretildi - yalnızca `.pdb` silmenin yetersiz olduğu netleşti (`.dll`/`.exe`'nin debug directory'si PDB'nin yerel derleme yoluna işaret ediyordu); `DebugType=None` ile PDB hiç üretilmediğinden bu işaretçi de oluşmuyor. Doğrulandı: pakette PDB yok, `Lens.*.dll`/`.exe` içinde yerel kullanıcı adı/yol dizesi yok (ikili tarama), `appsettings.json` boş şablon, model+bağımlılıklar mevcut, gerçek kullanıcı ayarı/ürün/index/log/test dosyası yok. Diğer üç publish paketi dokunulmadı | Yönetici talimatı (2026-09-04) - proje teslimi. Yeni özellik/kapsamlı yeniden tasarım eklenmedi. Kullanıcı ekranı o an aktif kullandığı için canlı UI otomasyonu/uzaktan tıklama YAPILMADI (bkz. proje talimatı) - 200 sonuçlu aramanın arayüzde akıcı kaydığı yalnızca kod incelemesi ve headless testlerle doğrulandı, gerçek ekranda henüz doğrulanmadı. Detay: `CHANGELOG.md`, `SimilaritySearch.MaxResults`, `MainWindow.SearchButton_Click` **⚠️ "Kullanıcıya yeni bir giriş alanı EKLENMEDİ" kısmı SUPERSEDED — bkz. #73 (yönetici kararı değişti, kullanıcının kendi seçtiği 1-200 arası "En fazla sonuç" girişi eklendi). 200 sabiti kendisi (artık teknik ÜST SINIR olarak) DEĞİŞMEDİ.** |
| 73 | **[Sorgu kilidi + kullanıcının sonuç sayısını belirlemesi]** İki küçük, kontrollü düzeltme: (1) **Sorgu kilidi:** Arama/index hazırlığı sürerken sorgu artık değiştirilemez - `SetBusy` akışına eksik olan `NewSearchButton` ve `MaxResultsTextBox` eklendi; ayrıca yalnızca görsel devre dışı bırakmakla YETİNİLMEDİ, `IsBusy` (yeni `_busyDepth` sayacına dayalı - ileride bir nested SetBusy çağrısı DIŞ işlemin korumasını erken kaldırmasın diye derinlik sayaçlı) kontrolü `NewSearchButton_Click` ve `QueryDropZone_DragEnter/DragOver/Drop` olay işleyicilerine de eklendi (savunma amaçlı ikinci katman - buton/alan zaten devre dışıyken bir olay yine de tetiklenirse sorgu yine değişmez). Meşgulken sürüklenen bir görsel kabul edilmez VE "kabul edilebilir" vurgusu hiç gösterilmez. `queryPath`/`threshold`/`maxResults` zaten arama başlangıcında local değişkenlere sabitleniyordu (bkz. #71/#72) - bu, alanların artık ayrıca devre dışı bırakılmasıyla ikinci bir savunma katmanı oldu. (2) **Kullanıcının sonuç sayısını belirlemesi:** "Minimum benzerlik (%)" yanına "En fazla sonuç" sayısal girişi eklendi (varsayılan 15, izin verilen aralık 1-200 tam sayı - yeni `Lens.Core.Search.MaxResultsPreference.TryParse`, `SimilarityThreshold` ile AYNI desen: harf/ondalık/negatif/0/200-üstü/boş reddedilir, sessizce başka bir sayıya ÇEVRİLMEZ). **[Revize - kullanıcı talimatı]** Yazma/yapıştırma sırasında girdi kısıtlaması UYGULANMADI (alan serbestçe düzenlenebilir) - doğrulama SADECE "Ara"ya basıldığında yapılır ve iki AYRI mesajla gösterilir: değer geçerli bir tam sayı ama 200'ü aşıyorsa "En fazla 200 sonuç listeleyebilirsiniz." (bkz. `MaxResultsPreference.IsAboveMaxAllowed`); boş/metin/ondalık/negatif/0 gibi diğer TÜM geçersiz durumlarda "Lütfen 1-200 arasında bir tam sayı girin." Her iki durumda da `ShowMaxResultsValidationError` + erken `return` - mevcut sonuçlar/karşılaştırma/kaydırma DOKUNULMAZ (threshold'un geçersiz girdi davranışıyla birebir aynı desen), kullanıcı geçerli bir değer girip tekrar aradığında uyarı otomatik kalkar (`HideMaxResultsValidationError`). Çekirdek katmanda ikinci bir doğrulama katmanı KORUNDU: `SimilaritySearch.SearchWithThreshold` 1-200 dışı bir `maxResults`'ta `ArgumentOutOfRangeException` fırlatır - sessiz dönüşüm yok. Geçerli tercih yalnızca arama BAŞARIYLA başlatıldığında (validasyon geçtiğinde) `UserSettings.PreferredMaxResults`'a yazılır (Load→değiştir→Save, tema/otomatik indeksleme/klasör tercihini KORUR); eski (alanı içermeyen) veya bozuk/aralık dışı kayıtlı bir değer `MaxResultsPreference.ValidateOrDefault` ile güvenle 15'e döner. Alan `SetBusy` ile işlem sürerken değiştirilemez. Arama sırası aynen korundu: eşik → azalan sıra → kullanıcının istediği sayı kadar al; yetersiz eşleşmede DOLDURMA yok. Başlık "EN BENZER SONUÇLAR" kaldı (sabit sayı eklenmedi), durum mesajı gerçek gösterilen sayıyı söylüyor (#72). **Test:** `Lens.AiProof hardeningtest` Grup J (39 kontrol: girdi validasyonu, `IsAboveMaxAllowed` mesaj ayrımı, `ValidateOrDefault`, `UserSettings.PreferredMaxResults` JSON sözleşmesi/geriye uyumluluk/alan korunumu, `SearchWithThreshold` limit=1/15/50/200 + yetersiz eşleşme + çekirdek katman `ArgumentOutOfRangeException`) eklendi - toplam 117/117 PASS. `dotnet build` Debug/Release 0 warning/0 error | Yönetici talimatı (2026-09-04) - teslim öncesi son iki düzeltme, küçük/kontrollü kapsam, yeni özellik/kapsamlı yeniden tasarım eklenmedi. Kullanıcı ekranı aktif kullandığı için canlı UI otomasyonu/masaüstü müdahalesi YAPILMADI - sorgu kilidinin (NewSearchButton/QueryDropZone busy-guard) ve yeni "En fazla sonuç" alanının gerçek ekranda akıp akmadığı/doğru devre dışı kaldığı yalnızca kod incelemesi ve headless testlerle doğrulandı, canlı ekranda henüz doğrulanmadı. Detay: `CHANGELOG.md`, `MainWindow.SetBusy`/`SearchButton_Click`/`NewSearchButton_Click`/`QueryDropZone_*`, `Lens.Core.Search.MaxResultsPreference`, `UserSettings.PreferredMaxResults` |
| 74 | **[Başlıkta gerçek sonuç sayısı]** "EN BENZER SONUÇLAR" başlığı artık parantez içinde ekranda GERÇEKTEN listelenen kart sayısını gösteriyor - ör. "EN BENZER SONUÇLAR (32)". Bu, #73'teki "başlığa sabit bir sayı eklenmedi" kararıyla ÇELİŞMEZ: gösterilen `(N)`, `SimilaritySearch.MaxResults` (200) gibi SABİT bir teknik üst sınır değil, `_results.Count`'un CANLI yansımasıdır (eşik + kullanıcının "en fazla sonuç" tercihi zaten uygulanmış, `IndexStatusText`'teki "N sonuç gösteriliyor." ile AYNI kaynak) - ör. 80 eşleşmeden limit nedeniyle 15'i listeleniyorsa "(15)" yazar, toplam eşleşme sayısı gibi SUNULMAZ. Yeni `MainWindow.UpdateResultsHeaderText()`, `_results` değiştiği HER noktada (ürün klasörü değişimi, "Yeni Arama", yeni sorgu görseli seçimi/sürükle-bırak, arama BAŞLANGICINDAKİ eski-sonuç temizleme, arama TAMAMLANDIĞINDA) çağrılır; geçersiz girdide (threshold/"en fazla sonuç" hatalı, erken `return`) bu metoda hiç ulaşılmadığından başlıktaki sayı AYNEN kalır - tıpkı `_results`'ın kendisi gibi. İlk açılışta XAML'de doğrudan "EN BENZER SONUÇLAR (0)" yazılı. Tema/kart seçimi/kaydırma davranışına dokunulmadı | Kullanıcı talimatı (2026-09-04) - son küçük arayüz değişikliği, tek dosya çifti (`MainWindow.xaml`/`.xaml.cs`). Kullanıcı ekranı aktif kullandığı için canlı doğrulama yapılmadı - kod incelemesiyle (tüm `_results.Clear()`/`_results.Add()` noktalarının `UpdateResultsHeaderText()` çağırdığı tek tek doğrulandı) ve build ile sınırlı kalındı. Detay: `CHANGELOG.md`, `MainWindow.UpdateResultsHeaderText` |
| 75 | **[Ayarlar sadeleştirme + klasör adresini elle girme]** Onaylı gereksinim: Ayarlar sade bir klasör-durumu ekranı olacak; teknik yollar/teknik ayrıntı bölümü ve dosya açma/silme/temizleme komutları olmayacak. Klasör işlemleri ana ekrandan, tema ⋮ → "Arka Plan" üzerinden yönetilmeye devam edecek; Ayarlar açmak/kapatmak tercih kaydetmeyecek. Adres kutusu normal metin düzenlemeyi destekleyecek; yazılan metin TASLAK, Enter doğrulayıp uygular (arama başlatmaz), Esc aktif adrese döner, odak kaybı tek başına uygulamaz. Uygulanmamış taslakta Ara/İndeksi Güncelle/Varsayılan Yap durmalı; işlem sürerken düzenleme kilitli, seçim/kopyalama mümkün olmalı. **Çalışma kopyasında yapılanlar:** `SettingsWindow`, düzenlenebilir `FolderPathTextBox`, `ProductFolderPathInput`, ortak `ApplyNewProductFolderAsync`, taslak/busy/başlangıç yüklemesi kontrolleri eklendi; Log Klasörünü Aç kaldırıldı. **AÇIK UYUŞMAZLIKLAR:** Teknik ayrıntılar bölümü hâlâ mevcut. Tam adres denetimi/kök adresin korunması, yükleme hatasında önceki durumun korunması ve aynı klasörü yeniden uygulamada varsayılan kaynağının değişmemesi henüz tamamlanmış kabul edilemez. Önceki metnin bu davranışları kesin çözülmüş gibi anlatan kısımları BU KAYITLA DÜZELTİLDİ; başlangıç yarışları ve ilk kopyalama şikâyeti de uçtan uca doğrulanmadı. | Kullanıcı talimatı (2026-09-04). Kullanıcı açık uyuşmazlıkların giderilmesini tasarım sonrasına ERTELEDİ; düzeltildiler anlamına gelmez. Kaynak commit/publish bekliyor; bu tur yalnızca dokümanlar gönderiliyor. Claude raporu: Grup L 13 biçim kontrolü dahil toplam 158/158 PASS, bu ilk turda Debug/Release 0 warning/0 error. Biçim testleri disk erişimi, başarısız geçiş, zamanlama ve canlı metin etkileşimini kanıtlamaz. Detay: `CHANGELOG.md`, `docs/PROJECT_CONTEXT.md` güncel devir notu. |
| 76 | **[Arama varsayılanları - 80/20]** Açılışta "Minimum benzerlik (%)" 80; "En fazla sonuç" mevcut geçerli kayıtlı tercihle, böyle bir tercih yoksa 20 (eski 15 varsayılanı yerine) ile dolu gelir. Kullanıcı bir alanı silip boş/yalnızca-boşluk bırakarak "Ara"ya basarsa o alan için AYNI varsayılan (80/20) kullanılır - ama mevcut `TryParse`'lerin KATI sözleşmesi (metin/negatif/aralık-dışı/NaN/Infinity reddi, sonuç sayısında "0" GEÇERSİZ) HİÇ DEĞİŞMEDİ; bu, ayrı ve test edilebilir yeni `SimilarityThreshold.ResolveOrDefault`/`MaxResultsPreference.ResolveOrDefault` adımlarına eklendi (yalnızca GERÇEKTEN boş/yalnızca-boşluklu girdiyi varsayılana çevirir, `TryParse`'in kendisi hâlâ boş girdiyi reddeder). Tek ortak sabit: `SimilarityThreshold.DefaultPercent=80` (yeni) hem açılış hem boş-girdi değeri; `MaxResultsPreference.Default` 15'ten 20'ye çıkarıldı ve zaten hem açılış/boş-girdi hem de "kayıtlı tercih yok/geçersiz" durumunda kullanılan TEK sabitti (üç kullanım noktası artık senkron). Mevcut GEÇERLİ kayıtlı tercihler (ör. 15, 50, 200) bu değişiklikten ETKİLENMEZ - yalnızca "tercih hiç yok/geçersiz/boş girdi" durumunda yeni varsayılan devreye girer. Boş girdiden çözülen değer, arama akışına alınan DİĞER her geçerli değer gibi `UserSettings.PreferredMaxResults`'a kaydedilir (mevcut mekanizma değişmedi). Doğrulama sonrası KULLANILAN değer yalnızca girdi BOŞSA ilgili kutuya geri yazılır (kullanıcının kendi geçerli girdisi - ör. "65" veya "80,5" - asla yeniden biçimlendirilip dokunulmaz). "Yeni Arama", tema değişimi ve klasör değişimi (#75) bu iki kutuya HİÇ dokunmuyor (kod incelemesiyle doğrulandı - üç yerde de bu alanlara yazma yok). Benzerlik tercihi için kalıcı bir `UserSettings` alanı BİLEREK EKLENMEDİ. #75'teki taslak/aktif-klasör-uyuşmazlığı ve işlem kilidi kontrollerine dokunulmadı. **Test:** `Lens.AiProof hardeningtest` Grup K (26 kontrol: threshold 0/65/80,5/100 geçerli + boş/null/boşluk→80 + metin/negatif/150/NaN/Infinity hâlâ reddedilir; maxResults 1/15/50/200 geçerli + boş/null/boşluk→20 + 0/negatif/201/ondalık/metin hâlâ reddedilir) eklendi; Grup J'deki YALNIZCA eski varsayılanı (15) sınayan `ValidateOrDefault`/JSON-yok senaryoları 20'ye güncellendi, GEÇERLİ kayıtlı 15'i sınayan J13 BİLEREK değiştirilmedi, iki yeni kontrol (J19b/J19c) kayıtlı 15/200'ün 20'ye topluca çevrilmediğini doğruluyor - toplam 158/158 PASS. `dotnet build` Debug/Release 0 warning/0 error | Yönetici talimatı (2026-09-04) - #75 ile aynı turda, mevcut değişiklikleri koruyarak eklendi. **SUPERSEDES #60 içindeki varsayılanın belirsiz/alanın boş olması ve #73 içindeki varsayılan 15/boş alanı reddetme davranışı; 1-200 sınırı aynen korunur.** Kaynak commit/publish bekliyor; test bilgisi Claude raporudur. Arama sıralaması/eşik filtresi/"en fazla" davranışı/200 teknik üst sınır/otomatik indeksleme varsayılanı (yeni/eksik ayarda true; mevcut kayıtlı false korunur) değiştirilmedi. Kullanıcı ekranı aktif kullandığı için canlı doğrulama yapılmadı - ResolveOrDefault mantığı headless test edildi, açılış görünümü ve "Ara" sonrası kutulara yazılan 80/20 değerleri yalnızca kod incelemesiyle doğrulandı. Detay: `CHANGELOG.md`, `SimilarityThreshold.DefaultPercent`/`ResolveOrDefault`, `MaxResultsPreference.Default`/`ResolveOrDefault`, `MainWindow.SearchButton_Click` |
| 77 | **[Onaylı taslağa göre görsel yerleşim düzeltmesi]** Önceki turun (#74/#75) eleman SIRASI korunarak ferahlık/oran/hiza taslağa uydurulacak şekilde yeniden düzenlendi. (1) Orta blok artık TEK dengeli grup: sorgu görseli → arama ayarları+Ara/Yeni Arama (tek alt `SettingsButtonsGrid`, İKİ bağımsız StackPanel/tahmini Margin DEĞİL) → seçilen sonuç; "Ara" ile eşik satırı, "Yeni Arama" ile sonuç-limiti satırı AYNI paylaşılan Grid satırında olduğundan dikey merkez hizası GARANTİ (pencere/font/doğrulama-mesajı durumundan bağımsız). (2) İki görsel eşit KARE oldu, geniş pencere hedefi 300×300 (önceki 260 yerine, **kullanıcının açıkça değiştirdiği karar** - taslakta yatay olsa da kare istendi); `Stretch="Uniform"` korunuyor. (3) Yeni `MainWindow.UpdateResponsiveLayout` (eski `UpdateComparisonImageSizes`'ın yerine geçti), görsel boyutu (200-300 DIP), ayar sütunu genişliği (240-260 DIP), Ara/Yeni Arama genişliği (104-120 DIP) ve üç boşluk (16-40 DIP) için TEK bir pencere-genişliği lineer interpolasyonu kullanır - farklı yerlerde çelişkili sabit YOK; Window.MinWidth (860) alt sınır, hesaplanan "tam geniş hedef toplamı" (~1136 DIP) üst sınırdır, ikisinin dışında CLAMP edilir (pencere büyüdükçe SINIRSIZ büyümez, fazla alan ComparisonRowGrid'in HorizontalAlignment=Center'i ile İKİ YANA dağılır). Giriş YÜKSEKLİKLERİ (36/42/34 DIP) BİLEREK sabit - yalnızca genişlikler daralır. Bir `RenderTransform`/`Viewbox` KULLANILMADI ki sürükle-bırak (`QueryDropZone_DragEnter/Over/Drop`) ve çift-tık büyütme (`TryOpenImagePreview`) davranışı hiçbir şekilde etkilenmesin - yalnızca `Width`/`Height` doğrudan atanıyor. (4) Üst iki satır artık TEK bir `TopAreaGrid` - "Bu Klasörü Varsayılan Yap" (satır0) ile "Sorunlu / Atlanan Dosyalar" (satır2) AYNI sütunu paylaşıyor, bu yüzden sol kenarları KESİN olarak aynı (talimat: "ikisi de sağa hizalı olması bu şartı karşılamaz" - iki bağımsız Grid'de garanti edilemeyecek bir şart olduğu için TEK Grid'e birleştirildi). Varsayılan buton grubunun sol başlangıcı, `TopAreaSpacerColumn`'ın ÖLÇÜLEN (tahmini sabit bir margin DEĞİL) genişliğiyle karşılaştırma satırındaki sağ görselin sol kenarı civarında tutuluyor. (5) Grid yapısı değiştiği için `ComparisonRowGrid`'in TÜM sütunları artık `x:Name` ile adlandırıldı (INDEKS DEĞİL - `ComparisonGapLeftColumn`, `SettingsColumn`, `SettingsButtonsGapColumn`, `ComparisonGapRightColumn`) ki ileride yapı tekrar değişirse yanlış sütun ölçülmesin/ayarlanmasın (talimatın açıkça uyardığı risk). (6) "Ürün Klasörü Seç" → "Tarama Klasörünü Seç" (buton içeriği, `OpenFolderDialog.Title`, Ayarlar'daki doğrudan atıf) - x:Name/olay işleyicisi/işlev DEĞİŞMEDİ, ilgisiz "ürün" ifadeleri toplu değiştirilmedi. Ertelenmiş konular (#75'teki "Teknik ayrıntılar", adres normalizasyonu, klasör geçişi durum-koruma sorunları) bu turda ELE ALINMADI - kapsam dışı bırakıldı. **Test (Claude raporu):** `dotnet build` Release 0 warning/0 error (Debug, kullanıcının açık uygulaması tarafından kilitliydi; son tasarım turunda henüz doğrulanamadı). `Lens.AiProof hardeningtest` 158/158 PASS (bu tur Lens.Core'a dokunulmadı - saf WPF yerleşim değişikliği, headless test kapsamı YOK) | **Durum:** Kaynak kod düzenlemesi rapora göre tamamlandı, görsel onay/Debug doğrulaması/kaynak commit/publish bekliyor; doküman commit'i bunları içermez. Kullanıcı talimatı (2026-09-04) - "senior WPF arayüz geliştiricisi" rolüyle, onaylı taslağa göre. Kullanıcı ekranı aktif kullandığı için CANLI GÖRSEL DOĞRULAMA YAPILMADI (uygulama açılmadı, otomasyon/fare/klavye müdahalesi yapılmadı) - bu, yalnızca kod incelemesi ve derleme ile sınırlı bir teslimdir; kullanıcının kendi ekran görüntüsüyle onayı BEKLENMEKTEDİR, "taslakla birebir eşleşti" iddiası yapılmadı. Model/arama algoritması/eşik/200 sınırı/tema/kartlar/arama kilitleri/normal indeksleme, #75'teki adres kutusu/varsayılan değer/kilit mantığı değiştirilmedi. Detay: `CHANGELOG.md`, `MainWindow.UpdateResponsiveLayout`, `MainWindow.xaml` (TopAreaGrid/ComparisonRowGrid/SettingsButtonsGrid) |
| 78 | **[Görsel yerleşim - orta form + üst klasör satırı taslak hizalaması + responsive sınır düzeltmeleri]** Üç küçük, ayrı düzeltme aynı kontrol noktasında commit edildi: (1) Orta arama formunda `ThresholdTextBox`/`MaxResultsTextBox` artık tahmini `Margin` yerine ortak bir Grid sütununda (`SettingsLabelColumn`/`SettingsInputColumn`) hizalanıyor; `SearchButton`/`NewSearchButton` aynı işlem sütununda. (2) Üst klasör satırında `MenuButton`, varsayılan klasör grubundan (`SetDefaultButton`/`ClearDefaultButton`) bağımsız hale getirildi ve `TopAreaGrid`'in son sütununa taşındı; menüden önce gerçek bir `*` sütun (`MenuSpacerColumn`) eklendi, bu sütun kalan tüm alanı doldurarak menüyü pencerenin sağına sabitliyor. `SetDefaultButton` ile `ProblemFilesButton` ortak sütunu (aynı sol kenar hizası) korundu. Klasör yolu kutusu (`FolderPathTextBox`) artık pencere genişliğine göre dar görünümde **180 DIP**'ten başlayıp geniş görünümde yaklaşık **530 DIP**'e kadar responsive büyüyor (XAML güvenlik sınırı `MinWidth=180`/`MaxWidth=560`) - eski sabit `MaxWidth="360"` kaldırıldı. (3) **Responsive sınır düzeltmeleri:** `UpdateResponsiveLayout` içinde aynı turda `FolderPathColumn.Width` atandıktan hemen sonra bir önceki layout turuna ait eski `FolderPathColumn.ActualWidth` okunması düzeltildi - `leftContentWidth` artık aynı turda hesaplanan, kolonun gerçek Min/Max sınırlarına clamp edilmiş `folderPathWidth` değerini kullanıyor (yeniden boyutlandırmada varsayılan buton grubunun bir kare geriden "sıçraması" giderildi). Dar yol hedefi 200'den 180 DIP'e (kolonun gerçek tabanı) indirildi; yeni adlandırılan `ProductInfoPanel` (ürün sayısı/kaynak `WrapPanel`'i) responsive bir `MaxWidth` aldı (dar görünümde ~160 DIP - `DirectorySourceText` gerekirse alt satıra kontrollü sarılır; geniş görünümde ~600 DIP, pratikte sınırsız, mevcut yan-yana görünüm korunur). Bu ikisi, 860 DIP minimum pencerede `Varsayılanı Temizle` görünürken `MenuButton`'u dışarı itme riskini `FormattedText` ile ölçülen ~118 DIP taşmadan ~35-45 DIP güvenlik payına indirdi. Mevcut varsayılan buton mesafesi (`DefaultGroupSpacerColumn` konumlandırma hesabı) bu kontrol noktasında BİLEREK korundu - "Bu Klasörü Varsayılan Yap" grubunu ürün/kaynak bilgisine yaklaştırma denemesi SONRAKİ, ayrı kullanıcı onaylı turdur. Arama/indeksleme mantığı, tema sistemi, 80/20 varsayılanları, adres kutusunun Enter/Esc davranışı ve Ayarlar penceresi DEĞİŞMEDİ. **SUPERSEDES #77'nin orta form (bağımsız `StackPanel` çifti) ve üst satır (7 sütunlu `TopAreaGrid`, sabit `MaxWidth="360"` yol kutusu) yerleşim DETAYLARI** - #77'nin kare görsel oranı, tek dengeli grup ortalaması, tema/arama/indeksleme kararları ve "Tarama Klasörünü Seç" adlandırması AYNEN GEÇERLİ kalır, yalnızca bu iki alt-yerleşimin somut uygulaması değişti | Kullanıcı talimatı (2026-09-07) - üç ayrı "kıdemli WPF geliştirici" turu (orta form, üst satır, responsive sınır düzeltmesi), aynı kontrol noktasında commit edildi. **AÇIK RİSKLER (canlı doğrulama bekliyor):** (a) 860×680 minimum pencerede, özellikle `Varsayılanı Temizle` görünürken üst satırda kesilme/taşma olmadığı henüz CANLI doğrulanmadı - yalnızca `FormattedText` ile yapılan çevrimdışı ölçüm ve kod incelemesiyle sınırlı kaldı (yukarıdaki ~35-45 DIP güvenlik payı tahmini pencere-çerçevesi kaybına dayanır, gerçek ekranda ölçülmedi). (b) `UpdateResponsiveLayout` içinde `ProductInfoColumn.ActualWidth` hâlâ bir önceki layout turunun ölçüsünü taşıyabilir (yalnızca `FolderPathColumn` için düzeltildi, `ProductInfoColumn` için kasıtlı olarak düzeltilmedi - o kolonun aynı turdaki kesin genişliği kod tarafından önceden bilinemez) - ilk açılış/sürekli yeniden boyutlandırmada varsayılan grubun konumu için etkisi küçük/geçici kabul edildi, ayrıca ölçülmedi. Bu riskler bu turda ÇÖZÜLMEDİ, yalnızca kaydedildi. Kullanıcı ekranı aktif kullanmadığından/uygulama açılmadığından canlı UI doğrulaması yapılmadı. Detay: `CHANGELOG.md` "Arama Formu ve Üst Klasör Satırı Hizaları", `MainWindow.UpdateResponsiveLayout`, `MainWindow.xaml` (`SettingsButtonsGrid`/`TopAreaGrid`/`ProductInfoPanel`) |
| 79 | **[Ekran uyumu - pencere ölçüsünün gerçek çalışma alanına sığdırılması]** Görsel tasarım DEĞİŞTİRİLMEDEN, 1920×1080/1366×768 ekranlarda %100/%125/%150 Windows ölçeklendirmesinde sabit `MinHeight=680`/`MinWidth=860`/başlangıç `Height=840`/`Width=1060` (DIP) değerlerinin gerçek çalışma alanına (görev çubuğu hariç) sığıp sığmadığı DIP aritmetiğiyle hesaplandı. **Tespit:** 1366×768 %125'te (~1093×574 DIP çalışma alanı) ve %150'de (~911×472 DIP) sabit `MinHeight=680` çalışma alanından BÜYÜK kalıyordu - WPF interaktif resize'da `MinHeight` altına asla izin vermediğinden kullanıcı pencereyi hiçbir şekilde küçültüp ekrana sığdıramıyor, sonuçlar/alt bilgi kalıcı erişilemez kalıyordu. Ayrıca 1366×768 %100'de (~1366×728 DIP) ve 1920×1080 %150'de (~1280×680 DIP) varsayılan başlangıç yüksekliği (840 DIP) çalışma alanını aşıyordu (ilk açılışta alt kenar ekran dışında/görev çubuğu altında kalabilir). 1920×1080 %100/%125 zaten güvenliydi. Bu, görevin "düzeltme gerekli" ölçütünü (`%125`/`%150`'de pencerenin altına erişilemiyor + `MinHeight` çalışma alanından büyük) açıkça karşıladığı için düzeltildi: yeni `MainWindow.ClampWindowToWorkArea()` (constructor'da `InitializeComponent()` sonrası çağrılır) `SystemParameters.WorkArea` (başlangıç anındaki mevcut ekran, DIP) gerçek min/başlangıç ölçülerinden küçükse bunları `Math.Min`/`Math.Clamp` ile YALNIZCA gerektiği kadar küçültür - hiçbir zaman büyütmez, normal/geniş ekranlarda (WorkArea zaten >860×680) hiçbir şey değişmez, onaylı 860×680/1060×840 birebir korunur. `WindowStartupLocation="CenterScreen"` eklendi (küçültülen pencere ortalanmış açılsın). `UpdateResponsiveLayout`'un görsel/sütun hesapları, orta bölüm/üst satır tasarımı, tema, arama/indeksleme mantığı DEĞİŞMEDİ - o metot zaten yalnızca `RootGrid.ActualWidth`'e göre çalıştığından küçültülen pencerede de aynı şekilde tepki verir. DPI-awareness manifestosu (PerMonitorV2 vb.) BİLİNÇLİ OLARAK EKLENMEDİ - incelenen 6 senaryo "tek ekranda başlangıç" durumudur, varsayılan (manifestosuz) WPF davranışı bu senaryolarda zaten doğru DIP hesabı yapar; yalnızca "açıkken farklı-DPI monitöre sürükleme" (kapsam dışı) durumunda bulanıklaşma riski taşır - eklenmedi çünkü gereksiz karmaşıklık olurdu | Kullanıcı talimatı (2026-09-07) - "farklı bilgisayarlarda ekran uyumu analizi ve gerekirse düzeltme" görevi, önce analiz + ölçülebilir risk teyidi + en küçük güvenli çözüm istendi. **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; `Lens.AiProof hardeningtest` 158/158 PASS (arama/indeksleme/doğrulama mantığına dokunulmadı, beklenen sonuç). Canlı ekran testi YAPILMADI (uygulama açılmadı) - hesap yalnızca kaynak kod + `SystemParameters.WorkArea`/DIP aritmetiği ile yapıldı, kullanıcının gerçek donanımda görsel doğrulaması önerilir. Çoklu monitör/DPI değişikliği (uygulama açıkken ekran değiştirme) kapsam dışı - `SystemParameters.WorkArea` yalnızca başlangıç anındaki birincil ekranı yansıtır. Detay: `CHANGELOG.md` "Ekran Uyumu — Pencere Ölçüsünün Çalışma Alanına Sığdırılması", `MainWindow.ClampWindowToWorkArea` |
| 80 | **[Assembly sürüm gösterimi + sayısal giriş sınırlaması]** İki bağımsız küçük düzenleme: (1) Ana pencere sol alt köşesinde (`Sürüm: 07.09.2026 — v1.0`) ve Hakkında ekranında gösterilen sürüm metninin TEK kaynağı artık `Lens.Desktop.csproj`'daki `InformationalVersion` MSBuild özelliği (SDK tarafından `AssemblyInformationalVersion` attribute'una otomatik derlenir, Visual Studio Project Properties/Assembly Information üzerinden yönetici tarafından değiştirilebilir) - XAML/C# içinde SABİT metin YOK. Yeni ortak `Lens.Desktop.AppVersionInfo.GetDisplayVersion()`, alt bilgi satırı VE Hakkında ekranı tarafından AYNI şekilde çağrılır (eski Hakkında ekranı yalnızca sayısal `GetName().Version` kullanıyordu, bu ayrım kaldırıldı - ikisi artık asla farklı sürüm gösteremez). `AssemblyVersion`/`FileVersion` ayrı, standart sayısal `1.0.0.0` biçiminde tutuldu (duplicate assembly attribute riski yok, mevcut `AssemblyInfo.cs`'deki WPF `ThemeInfo` tanımına dokunulmadı); `IncludeSourceRevisionInInformationalVersion=false` ile olası bir gelecekteki SourceLink paketi git commit hash'ini sürüm metnine otomatik eklemez. Alt bilgi satırı artık 2 sütunlu responsive `Grid` (sol: sürüm, sağ: mevcut açıklama metni değişmeden) - 860 DIP minimumda kesilme/üst-üste-binme yok. (2) "Minimum benzerlik (%)"/"En fazla sonuç" alanlarına artık harf/geçersiz karakter yazılamıyor/yapıştırılamıyor - yeni, WPF'siz test edilebilir `Lens.Core.Search.NumericInputFilter` karakter-düzeyi karar verir; `MainWindow`'da paylaşılan `PreviewTextInput`+`DataObject.Pasting`+`TextChanged`(son güvenlik ağı) üç olay işleyicisi bu mantığı HER İKİ alana da uygular. Minimum benzerlik TR virgülü/nokta ile ondalık kabul eder, en fazla sonuç YALNIZCA tam sayı kabul eder (ayırıcı reddedilir); eksi işareti ve harf her iki alanda da HER ZAMAN reddedilir. Bu filtre yalnızca KARAKTER düzeyinde çalışır - `SimilarityThreshold`/`MaxResultsPreference`'in 0-100/1-200 aralık doğrulaması, boş girdide 80/20 varsayılanı ve "200'ü aşan" uyarı mesajı HİÇ DEĞİŞMEDİ (aralık dışı ama karakter-geçerli bir sayı, ör. "101"/"201", filtreden geçer, asıl doğrulama katmanında ele alınmaya devam eder) | Kullanıcı talimatı (2026-09-07) - "kıdemli .NET 8/WPF geliştiricisi" rolüyle, iki son düzenleme. **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; `Lens.AiProof hardeningtest` 192/192 PASS (önceki 158 + yeni Grup M: `NumericInputFilter`, 34 kontrol). Derlenen DLL'in Win32 `ProductVersion`/`FileVersion` kaynakları reflection/VersionInfo ile doğrulandı - em-tire (—, U+2014) dahil metin birebir korunuyor. Canlı görsel doğrulama YAPILMADI (uygulama açılmadı) - sol alt köşe/Hakkında/sayısal alan davranışının kullanıcı tarafından gerçek ekranda kontrolü bekleniyor. Publish paketi BİLİNÇLİ OLARAK güncellenmedi (görsel kabul öncesi). Detay: `CHANGELOG.md` "Assembly Sürüm Gösterimi ve Sayısal Giriş Sınırlaması", `docs/DEPLOYMENT.md` §9, `MainWindow.AppVersionInfo`/`NumericInputFilter`/`NumericTextBox_*` |
| 81 | **[Küçük arayüz düzeltmeleri + 3-rakam sınırı]** Dört bağımsız küçük, belirtilen alana sınırlı düzenleme: (1) Tema alt menüsünde "Lime (Deneme)" → "Lime" (`MainWindow.xaml` `MenuItem.Header` + tutarlılık için `ThemePalette.DisplayName`, tema renk paleti değişmedi). (2) "Ayarlar" menü öğesi ve pencere başlığı → "Bilgilendirme" (kullanıcıya görünen HER İKİ yerde tutarlı); `SettingsWindow` sınıf/dosya adı ve `SettingsMenuItem_Click` metot adı BİLEREK değiştirilmedi (kod tarafı zorunlu değildi). (3) Bilgilendirme ekranının varsayılan kapalı "Teknik ayrıntılar" bölümüne, yalnızca geliştiricilere yönelik kısa bir "Versiyonlama" notu eklendi - sürümün `Lens.Desktop.csproj`'daki `AssemblyVersion`/`FileVersion`/`InformationalVersion`'dan geldiğini (bkz. karar #80), önerilen `MAJOR.MINOR.PATCH` artırma kuralını ve yayın öncesi sürüm güncelleyip yeniden derlemenin gerekliliğini anlatıyor - salt bilgi, komut/eylem içermiyor. (4) "Minimum benzerlik (%)"/"En fazla sonuç" alanlarına artık en fazla 3 rakam (ondalık ayırıcı hariç, ayırıcının iki tarafı BİRLİKTE sayılır) girilebiliyor - `NumericInputFilter`'a `MaxDigitCount=3` sabiti eklendi, hem karakter-düzeyi ret (`IsValidPartialText`, klavye/üzerine-yazma/yapıştırma) hem son güvenlik ağı (`StripInvalidCharacters`) bu sınırı uygular; 4+ rakamlı bir YAPIŞTIRMA kesilip kısaltılmaz, TAMAMEN reddedilir. "201"/"9999" gibi 3 rakamlı ama aralık-dışı bir değer hâlâ yazılabilir - "Ara"da mevcut "200'ü aşan" uyarısı DEĞİŞMEDEN devam eder | Kullanıcı talimatı (2026-09-07) - aynı günün iki küçük ek turu (ek görev + ek istek), tek kontrol noktasında birlikte commit edildi. **Test:** `dotnet build Lens.sln -c Release` 0 warning/0 error (Debug, kullanıcının açık `Lens.Desktop.exe`'si tarafından kilitliydi - müdahale edilmedi). `Lens.AiProof hardeningtest` 204/204 PASS (192 önceki + Grup M'e eklenen 12 yeni kontrol M35-M46). Canlı görsel doğrulama YAPILMADI (uygulama açılmadı) - Lime etiketi/Bilgilendirme başlığı-notu/3-rakam sınırının kullanıcı tarafından gerçek ekranda kontrolü bekleniyor. Publish paketi BİLİNÇLİ OLARAK güncellenmedi. Detay: `CHANGELOG.md` "Küçük Arayüz Düzeltmeleri ve 3-Rakam Sınırı", `SettingsWindow.xaml`, `Lens.Core.Search.NumericInputFilter.MaxDigitCount` |
| 82 | **[DENEY - sorgu/arama ayarları yerleşimi]** **Durum: `codex/query-settings-layout-experiment` branch'inde uygulanmıştır; canlı görsel kabul ve `main`'e merge beklemektedir - bu satır `main`'de yayınlanmış/kalıcı bir karar ANLATMAZ.** Ayrı "Sorgu Görseli Seç" butonu kaldırıldı, yerine üstte eşit genişlikte `Ara`/`Yeni Arama` yan yana yerleşti; boş sorgu çerçevesi artık kendisi tıklanabilir/klavye-erişilebilir (tek tık/Enter/Space dosya seç, görsel varken tek tık yalnızca odaklanır, çift tık her zaman önizler - dosya seçme kodu `MainWindow.OpenQuerySelectDialog`'a çıkarıldı, kopyalanmadı). Sorgu çerçevesi içine iki satırlı, yarı saydam, `IsHitTestVisible=False` bir boş-durum watermark'ı ("Sorgu görselini seçin" / "Tıklayın veya buraya sürükleyin") ve Seçilen Sonuç çerçevesine "Henüz sonuç seçilmedi" placeholder'ı eklendi - ikisi de çerçeve zemini her temada SABİT BEYAZ kaldığından (mevcut, dokunulmayan karar) mevcut `SecondaryTextBrush` yerine yeni, tema-bağımsız `CardSecondaryTextBrush` kullanır (kullanıcı onayıyla, bkz. görev talebi). Çerçeve altındaki kalıcı "Görsel seçin veya buraya sürükleyin • çift tık: büyüt" bilgisi bilerek KORUNDU. Minimum benzerlik/en fazla sonuç/otomatik indeksleme kutucuğu artık "ARAMA AYARLARI" başlıklı, ince kenarlıklı bir panel içinde; panel yüzeyi/kenarlığı `AppTheme.cs`/`ThemePalette`'e (tema paletleri) DOKUNULMADAN, `MainWindow.SetTheme` içinde mevcut ana pencere renginden (`BlendToward` ile siyaha %12 harmanlanarak) türetilir. Sayısal giriş kutuları (`ThresholdTextBox`/`MaxResultsTextBox`) 84 DIP'ten sabit 56 DIP'e daraltıldı (pencere genişliğiyle artık değişmiyor - talimatta bu kutu için ayrı dar/geniş tablo verilmediği için bilinçli olarak lerp edilmedi); `NumericInputFilter` DEĞİŞTİRİLMEDİ. **Bu karar, #77'deki 300×300 KARE görsel oranını (ve #77/#78'in bu orana bağlı `SettingsLabelColumn`/`SettingsInputColumn`/buton-genişliği yerleşim DETAYLARINI) 320×240 (4:3, dar pencerede ~240×180'e küçülen) yatay dikdörtgenle DENEYSEL olarak supersede etmeyi ÖNERİR - kullanıcı canlı görsel kabul verip merge onaylamadan bu supersede KALICI/NİHAİ ilan edilmez.** `MainWindow.UpdateResponsiveLayout` artık yalnızca pencere genişliğine değil (`TopAreaGrid`/`FooterGrid` gerçek ölçülmüş yüksekliği + XAML yapısından statik tahmin edilmiş sabitlerle - CANLI DPI ölçümü DEĞİL), kullanılabilir yüksekliğe de bakarak görsel genişliğini (ve ondan türetilen 4:3 yüksekliği) sınırlıyor; sonuç alanı (`EN BENZER SONUÇLAR`) için asgari bir rezerv (120 DIP) her zaman korunuyor. Orta blok üst/alt margin'i 32/20'den 20/12'ye küçültüldü. Değiştirilmeyenler (görev kapsamı dışı bırakıldı): CLIP modeli, embedding/benzerlik/eşik mantığı, sonuç sıralaması, 200 üst sınırı, %80/20 varsayılanları, index formatı/`.lens` konumu, otomatik index mantığı, kullanıcı ayar dosyası, klasör seçme/varsayılan klasör davranışları, üst klasör satırı yerleşimi (#78), tema paletlerinin KENDİSİ, alt bilgi sürüm sistemi, sonuç kartlarının veri modeli, busy kilidi, arama sonrası kaydırma kuralları | Kullanıcı talimatı (2026-09-08) - "kıdemli .NET 8/WPF masaüstü arayüz geliştiricisi" rolüyle, `codex/query-settings-layout-experiment` deney branch'inde, `main`'e DOKUNMADAN. **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; `Lens.AiProof hardeningtest` 204/204 PASS (bu tur Lens.Core'a dokunulmadı - saf WPF yerleşim değişikliği). `git diff --stat` yalnızca `MainWindow.xaml`/`MainWindow.xaml.cs`. Canlı görsel doğrulama YAPILMADI (talimat gereği kullanıcı izni olmadan uygulama açılmadı) - watermark görünürlük geçişleri, tek/çift tık ayrımı, 4:3 responsive hesap ve panel renk kontrastı yalnızca kod incelemesi + statik tahminle doğrulandı, kullanıcının kendi ekranında (özellikle 860×680 minimum pencere ve yüksek DPI ölçeklendirme) kontrolü BEKLENİYOR. Detay: `CHANGELOG.md` "[DENEY - Sorgu/Arama Ayarları Yerleşimi]", `MainWindow.UpdateResponsiveLayout`/`OpenQuerySelectDialog`/`QueryDropZone_KeyDown`/`SetTheme`/`BlendToward` |
| 83 | **[DENEY - arama paneli görsel polish]** **Durum: `codex/query-settings-layout-polish` branch'inde (karar #82'yi TEMEL ALARAK) deneysel olarak uygulanmıştır; canlı görsel kabul ve `main`'e merge beklemektedir - bu satır `main`'de yayınlanmış/kalıcı bir karar ANLATMAZ.** Altı sınırlı düzeltme: (1) ARAMA AYARLARI paneli renkleri artık ana pencere zemininden `BlendToward` ile TÜRETİLMİYOR (Lime'da kirli sonuç veriyordu) - `MainWindow.GetSettingsPanelColors`, her tema için AYRI SABİT bir yüzey/yazı/ikincil-yazı/kenarlık dörtlüsü döndürüyor (talimattaki hex değerler); `AppTheme.cs`/`ThemePalette`'e DOKUNULMADI, yalnızca `SettingsPanelBackgroundBrush`/`SettingsPanelForegroundBrush`/`SettingsPanelSecondaryTextBrush`/`SettingsPanelBorderBrush` kaynakları `MainWindow.SetTheme` içinde bu tablodan atanıyor; panel başlığı/etiketleri/checkbox metni artık `NeutralTextBrush` DEĞİL, panelin KENDİ foreground kaynağını kullanıyor (aksi halde Normal/Koyu temada beyaz metin beyaza yakın panelde kaybolurdu). (2) `ThresholdTextBox`/`MaxResultsTextBox` artık sağlarında iki `RepeatButton` (▲/▼, 18 DIP ok alanı, toplam kontrol 76 DIP) bulunan bir NumberBox görünümünde - yeni NuGet paketi EKLENMEDİ, mevcut `TextBox` (filtre/olay işleyicileri AYNEN korunarak) + `RepeatButton` kompozisyonu kullanıldı. Yeni `MainWindow.StepThreshold`/`StepMaxResults`, mevcut `SimilarityThreshold`/`MaxResultsPreference` sözleşmesini (0-100/1-200, virgül/nokta, boş→80/20) ÇAĞIRIR - yeni bir paralel doğrulama mantığı YAZILMADI; aralık-dışı bir metin (ör. karakter-filtresi izin verdiği ama aralık-dışı "999") güvenli sınıra `Clamp` edilir, exception atmaz. Yukarı/Aşağı ok tuşları (`NumericTextBox_PreviewKeyDown`) aynı adımı uygular; `RepeatButton`'lar `SetBusy` ile TextBox'larla birlikte devre dışı kalır. `NumericInputFilter` DEĞİŞTİRİLMEDİ. (3) Sorgu boş-durum yazıları büyütüldü: ana satır 15→18(dar)-22(geniş) DIP + opacity 0.55→0.64, alt satır 11→12(dar)-13(geniş) DIP + opacity 0.55→0.62 (`QueryEmptyTitleText`/`QueryEmptyHintText`, `UpdateResponsiveLayout` içinde aynı "t" ile lerp edilir). (4) Panel iç boşlukları ferahlatıldı: `Padding` 14,10,14,12 → 16 (tüm kenarlar), satır araları 8/8/10 → 13/12/14 DIP. (5) **Eski hesap hatası düzeltildi:** Orta grup genişliği artık eski `settingsColumnWidth + gapInner + buttonWidth` formülüyle DEĞİL (dar ~360/geniş ~412 DIP, gereğinden uzun butonlar üretiyordu), doğrudan bir hedefle (`narrowMiddleColumn`=280 → `wideMiddleColumn`=320) hesaplanıyor; görsel-panel dış boşluğu da 16/40 → 24(dar)/50(geniş) DIP hedefine çekildi - ikisi de AYNI "t" ile. Artık kullanılmayan `narrowSettingsColumn`/`wideSettingsColumn`/`narrowButtonWidth`/`wideButtonWidth`/`gapInner`/`wideGapInner` kaldırıldı. (6) **Yatay taşma güvenliği + yükseklik-tabanlı görsel küçültme çelişkisi düzeltildi:** Önceki turda `Math.Clamp(..., narrowImage=240, wideImage)` nedeniyle görsel genişliği yükseklik bütçesi ne kadar dar olursa olsun 240'ın ALTINA inemiyordu - "sonuç alanına her zaman 120 DIP ayrılıyor" iddiası bu yüzden GERÇEK ANLAMDA garanti edilmiyordu. Artık mutlak güvenli taban 200 DIP (`absoluteFloorImage`) - 240, yalnızca normal responsive HEDEF, mutlak sınır değil. Ayrıca her `UpdateResponsiveLayout` turunda toplam orta grup genişliği (`imageWidth*2 + gapOuter*2 + middleColumnWidth`) `RootGrid.ActualWidth` ile karşılaştırılıyor; taşarsa talimattaki sırayla (1) dış boşluk → (2) orta sütun → (3) görsel (4:3 korunarak, 200 DIP tabana kadar) küçültülüyor - negatif/çok küçük alanlarda exception/taşma oluşmaz. Değiştirilmeyenler (görev kapsamı dışı bırakıldı): 4:3 görsel kararı, eşit görsel boyutu, tıklama/sürükle-bırak/çift tık davranışı, çerçeve içi/altı yönlendirme metinleri, Ara/Yeni Arama işlevleri, busy kilidi, benzerlik algoritması/eşik doğrulaması/sonuç sıralaması, 200 üst sınırı, %80/20 varsayılanları, index/`.lens` yapısı, model, üst klasör satırı, sonuç kartları, kaydırma sıfırlama kuralları, sürüm/Assembly sistemi, tema adları ve ANA tema renkleri (`ThemePalette` değerleri) | Kullanıcı talimatı (2026-09-08) - "Arama Paneli Görsel Polish Deneyi", `codex/query-settings-layout-polish` branch'inde, `codex/query-settings-layout-experiment` ve `main`'e DOKUNMADAN. **Test:** `git diff --check` temiz; `dotnet build Lens.sln` Debug/Release 0 warning/0 error (Debug'da geçici bir dosya-kilidi UYARISI oluştu - başka bir `MSBuild.exe` süreci `Lens.Core.dll`'i kilitliyordu, ama build otomatik retry ile YİNE DE 0 hata ile tamamlandı, süreç sonlandırılmadı); `Lens.AiProof hardeningtest` 204/204 PASS (Lens.Core'a dokunulmadı). Canlı görsel doğrulama YAPILMADI (talimat gereği kullanıcı izni olmadan uygulama açılmadı/otomasyon çalıştırılmadı) - panel renk kontrastı, spinner davranışı (basılı-tutma tekrarı, boş/aralık-dışı değer güvenliği), watermark boyutu ve responsive taşma hesabı yalnızca kod incelemesi + statik bir hesap tablosuyla (bkz. son rapor - 860×680/1060×840/1366×768/1920×1080 için ActualWidth/ActualHeight ~36/64 DIP çerçeve+margin VARSAYIMIYLA tahmin edildi, CANLI ölçüm DEĞİL) doğrulandı; kullanıcının kendi ekranında (özellikle 860×680 minimum pencere, Koyu/Koyu Sepya panel kontrastı ve yüksek DPI) kontrolü BEKLENİYOR. Detay: `CHANGELOG.md` "[DENEY - Arama Paneli Görsel Polish]", `MainWindow.GetSettingsPanelColors`/`StepThreshold`/`StepMaxResults`/`ResolveSteppableValue`/`FormatSteppedNumber`/`UpdateResponsiveLayout` |
| 84 | **[ClickOnce kurulum ve otomatik güncelleme]** `feature/clickonce-deployment` branch'inde, `Lens.Desktop` için kaynak kontrollü bir ClickOnce publish profili (`src/Lens.Desktop/Properties/PublishProfiles/ClickOnce.pubxml`) eklendi: kullanıcı bazlı kurulum (`setup.exe`), self-contained (win-x64, hedef makinede .NET kurulu olmasına gerek yok), Başlat menüsü kısayolu, çevrimdışı çalışma, `UpdateEnabled=true`/`UpdateMode=Foreground` ile açılışta güncelleme kontrolü. Uygulama adı `Lens`; `PublisherName` **açık placeholder** (`[PLACEHOLDER] Yayıncı Adı Belirlenmedi`) — gerçek şirket adı uydurulmadı. `InstallUrl`/`UpdateUrl` **bilerek boş** — gerçek UNC/HTTPS adresi bilinmediğinden yerel/offline staging publish (`publish/ClickOnce/`, `.gitignore`'lu) üretildi, uydurma/Google Drive adresi KULLANILMADI. `SignManifests=false` — gerçek şirket kod imzalama sertifikası yok, sahte/test sertifikası oluşturulmadı veya sertifika deposuna yüklenmedi. ClickOnce'ın 4 parçalı `ApplicationVersion`'ı ayrı bir sayı DEĞİL, mevcut `FileVersion`'dan (`$(FileVersion)`) MSBuild property referansıyla türetiliyor — tek sürüm kaynağı korunuyor (karar #80 ile tutarlı). Model (`models/clip-vision-b16-openai.onnx`) ve `appsettings.json`, ClickOnce'ın klasik hedeflerinin yalnızca `Content` öğelerini (uzantısız/`.exe`/`.dll` olmayan `None` öğelerini DEĞİL) manifest'e koşulsuz dahil ettiği yerinde test edilerek keşfedildi — bu iki öğe `None`'dan `Content`'e taşındı (normal `dotnet publish`/taşınabilir publish davranışını ETKİLEMEDİ, yerinde doğrulandı). ClickOnce publish'i `dotnet publish`/`dotnet msbuild` (.NET Core MSBuild) ile ÇALIŞMIYOR (`MSB4803`) — tam .NET Framework MSBuild (bu makinede Visual Studio 18 Community) gerekiyor; bu, `/p:DebugType=none` global property'sinin proje referansı (`Lens.Core`) için de komut satırında AYRICA verilmesi gerektiğini (aksi halde `Lens.Core.dll` içine derleme makinesinin yerel yolu gömülü kalıyordu — yerinde tespit edilip düzeltildi) beraberinde getirdi. **Aynı/düşük `FileVersion` ile aynı `PublishDir`'e tekrar publish MSBuild tarafından REDDEDİLMİYOR** (yerinde test edilerek doğrulandı, önceki varsayımın aksine) — sürüm geriye düşmesine karşı tek koruma yayın öncesi elle disiplindir, otomatik güvenlik ağı yoktur. Mevcut taşınabilir self-contained publish akışı (`docs/DEPLOYMENT.md` §1) DEĞİŞMEDİ, yerinde doğrulandı. Detay/tam gerekçe: `docs/CLICKONCE.md` | Kullanıcı talimatı (2026-09-08). **Üretim tamamlanmış SAYILMAZ** — bkz. "Not Yet Decided" #9-11 (yayıncı adı, güncelleme adresi, kod imzalama sertifikası). **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; `Lens.AiProof hardeningtest` 204/204 PASS (Lens.Core'a dokunulmadı); taşınabilir `dotnet publish -r win-x64` ayrıca doğrulandı; ClickOnce publish çıktısı (`setup.exe`, `.application`, `.manifest`, model, `appsettings.json` dahil) elle incelendi — `.pdb`, yerel kullanıcı yolu, ürün görseli, `.lens`/index/log dosyası bulunmadı. Canlı kurulum/güncelleme penceresi kullanıcı izni olmadan açılmadı (bkz. `docs/CLICKONCE.md` §12, en fazla 3 canlı test adımı orada listelidir). |
| 85 | **[ClickOnce masaüstü kısayolu]** `ClickOnce.pubxml`'e `<CreateDesktopShortcut>true</CreateDesktopShortcut>` eklendi (`Install`/`InstallFrom`/çevrimdışı kurulum ayarlarıyla aynı blokta, karar #84'ün profiline ek). Kurulum tamamlanınca Başlat menüsü kaydı (ClickOnce'ın zaten varsayılan davranışı, değişmedi) yanında kullanıcının masaüstünde otomatik `Lens` kısayolu oluşur; görev çubuğuna sabitleme yapılmaz (ClickOnce'ta böyle bir ayar yok). Kısayol, publish klasöründeki EXE'ye değil `Lens.Desktop.application` deployment manifestine işaret eder — sürüm güncellemelerinde geçerliliğini korur. Yerinde doğrulandı: publish sonrası üretilen deployment manifestinde `co.v1:createDesktopShortcut="true"` özniteliği mevcut. Mevcut güncelleme/self-contained/model/sürüm/kullanıcı ayarları davranışları DEĞİŞMEDİ. Detay: `docs/CLICKONCE.md` §2b | Kullanıcı talimatı (2026-09-08). **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; ClickOnce publish (tam .NET Framework MSBuild, `/p:DebugType=none`) başarılı; deployment manifestinde `createDesktopShortcut="true"` doğrulandı; `setup.exe`/`.application`/`Application Files`/model/`appsettings.json` mevcut; `.pdb`/yerel yol/`.lens`/log/ürün görseli/gerçek kullanıcı ayarı bulunmadı; taşınabilir `dotnet publish -r win-x64` ayrıca doğrulandı, değişmedi. Canlı kurulum penceresi kullanıcı izni olmadan açılmadı. |
| 86 | **[Görünen sürüm tarihi iki haneli yıla geçirildi]** `Lens.Desktop.csproj` → `InformationalVersion` `07.09.2026 — v1.0`'dan `08.09.26 — v1.0`'a değiştirildi; `v1.0` etiketi AYNEN korundu. Görünen sürümün TEK kaynağı (`AppVersionInfo.GetDisplayVersion()`, alt bilgi satırı + Hakkında ekranı) DEĞİŞMEDİ — XAML/C# içine sabit metin YAZILMADI, yalnızca csproj'daki değer güncellendi. Tarih otomatik/derleme-zamanlı hesaplanmıyor, yönetici her sürümde elle günceller (bkz. `docs/DEPLOYMENT.md` §9, artık "Zorunlu biçim: `GG.AA.YY — vX.Y`" olarak açıkça belirtildi — dört haneli yıl YAZILMAMALI). `AssemblyVersion`/`FileVersion` (`1.0.0.0`) ve ClickOnce `ApplicationVersion`/güncelleme davranışı bu görev kapsamında DEĞİŞMEDİ — ikisi `InformationalVersion`'dan bağımsız ayrı MSBuild özellikleridir (bkz. `docs/CLICKONCE.md` §4, yeni eklenen "iki farklı sürüm kavramı" uyarısı). Kültür/sistem-bağımlı yeniden biçimlendirme EKLENMEDİ — assembly metadata'sındaki metin olduğu gibi (culture-invariant sabit string) gösterilir. Detay: `docs/DEPLOYMENT.md` §9, `docs/CLICKONCE.md` §4 | Kullanıcı talimatı (2026-09-08). **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; `Lens.AiProof hardeningtest` 204/204 PASS; derlenen `Lens.Desktop.dll`'de `InformationalVersion` metninin tam olarak `08.09.26 — v1.0` olduğu (eski `07.09.2026` değerinin KALMADIĞI) binary inceleme ile doğrulandı; ClickOnce paketi yeniden üretildi — deployment manifestinde hem `co.v1:createDesktopShortcut="true"` (karar #85) HEM yeni sürüm metni birlikte doğrulandı, `ApplicationVersion` `1.0.0.0` olarak DEĞİŞMEDİ; taşınabilir `dotnet publish -r win-x64` ayrıca doğrulandı, yeni sürüm metnini taşıyor. Canlı arayüz kullanıcı izni olmadan açılmadı. |
| 87 | **[Lens uygulama ve ClickOnce ikonu eklendi]** Lime yeşili yuvarlatılmış zemin, koyu lacivert büyüteç, kırmızı/mercan tonlarında beş yapraklı çiçek desenli (sarı-altın merkez, yazı/harf yok) resmi uygulama ikonu eklendi. **Kaynak notu:** Görev sırasında kullanıcı final tasarımı değiştirdi — önce sunulan geometrik/mavi-beyaz örgülü varyant yerine bu çiçekli varyant final olarak seçildi; geometrik varyanttan üretilen ara çıktılar (master PNG, ilk `Lens.ico`) hiçbir yere bağlanmadan/commit edilmeden silinip iş çiçekli kaynaktan baştan yapıldı. Kullanıcının onayladığı kaynak PNG (`lens-app-icon-*-source.png`, 1254×1254, 24bpp RGB, damalı SAHTE şeffaflık — gerçek alpha yok) doğrudan `.ico`'ya çevrilmedi; bağlantılı-bileşen flood-fill ile gerçek arka plan tespit edilip RGBA'ya çevrildi, kenar bandı "decontamine" edilerek gri/beyaz hale önlendi — `src/Lens.Desktop/Assets/lens-app-icon-master.png` (temiz ana kaynak, yalnızca geliştirme zamanı, derleme/publish çıktısına DAHİL DEĞİL) ve ondan üretilen `src/Lens.Desktop/Assets/Lens.ico` (16/20/24/32/40/48/64/128/256 px, her biri teknik olarak doğrulanmış gerçek bitmap karesi — yalnızca etiket değil) final varlıklar olarak eklendi. Damalı kaynak referans PNG'ler commit'e alınmadı, gerekmedikleri için diskten silindi. `Lens.Desktop.csproj`'a `ApplicationIcon` (EXE Win32 ikonu) ve `Resource` (WPF pencere başlığı ikonu için pack-URI) eklendi — **tek** fiziksel `Lens.ico` dosyasından, gereksiz kopya oluşturulmadan. Projedeki **tüm** pencereler (`MainWindow`, `AlertWindow`, `ImagePreviewWindow`, `ProblemFilesWindow`, `SettingsWindow`) `Icon="/Assets/Lens.ico"` ile aynı ikonu kullanır — hiçbiri Windows varsayılanına dönmez. ClickOnce masaüstü kısayolu (karar #85) ve Başlat menüsü kaydı ayrı bir ikon ayarı OLMADAN aynı EXE ikonunu miras alır; `setup.exe`/`Launcher.exe` için ayrı ikon atanmadı (öncelik kurulan uygulama/kısayollardır). Pencere düzenine, temalara, arama davranışına dokunulmadı. Detay: `docs/CLICKONCE.md` §2c | Kullanıcı talimatı (2026-09-08, tasarım kaynağı görev ortasında değiştirildi). **Test:** `dotnet build Lens.sln` Debug/Release 0 warning/0 error; `Lens.AiProof hardeningtest` 204/204 PASS; ana PNG'nin gerçek RGBA alpha'sı ve 4 köşenin alpha=0 olduğu + opak piksellerde gri/beyaz hale KALMADIĞI numuneleme ile doğrulandı; ICO'daki 9 boyutun tamamının gerçek, birbirinden farklı bitmap karesi olduğu (`Image.info['sizes']` + merkez piksel karşılaştırması) doğrulandı; derlenmiş `Lens.Desktop.exe`'nin özel ikonu taşıdığı `System.Drawing.Icon.ExtractAssociatedIcon` ile doğrulandı; ClickOnce paketi (temiz `bin`/`obj` + `/p:DebugType=none`) yeniden üretildi — deployment manifestinde `co.v1:createDesktopShortcut="true"` KORUNDU, model/`appsettings.json`/`setup.exe`/`.application` mevcut, `.pdb`/yerel yol/gerçek kullanıcı ayarı/log/index/ürün görseli YOK; taşınabilir `dotnet publish -r win-x64` ayrıca doğrulandı, değişmedi (ikon dosyası publish çıktısına loose dosya olarak kopyalanmıyor, yalnızca EXE'ye gömülü). Canlı arayüz/kurulum penceresi kullanıcı izni olmadan açılmadı. |

---

## Not Yet Decided (Henüz Alınmamış Kararlar)

Bu konularda **kesin hüküm verilmemiştir**.

| # | Konu | Neden Açık |
|---|------|------------|
| 1 | **Production** için final model seçimi: CLIP mi SigLIP mi, yoksa başka bir model mi | MVP için CLIP provisional olarak seçildi (karar #20); production için kesin değil, ileride yeniden değerlendirilebilir |
| 2 | Dış AI servislerine (cloud API) izin verilip verilmeyeceği | PoC kapsamında kullanılmayacağı netleşti (karar #6); ileri faz için hâlâ açık |
| 3 | ~~Gerçek fabrika veritabanı DBMS türü~~ | **Artık ilgisiz** — karar #31 ile görsel kaynağı olarak DB kalıcı olarak kapsam dışı bırakıldı |
| 4 | Çoklu kullanıcı / eşzamanlılık gereksinimleri | İleri faz, detay netleşmedi. Not: dosya erişimi tarafında kısmi cevap var (bkz. karar #39 — Recommended, local cache) |
| 5 | Test/benchmark metodolojisi (sentetik varyasyonların temsil gücü) | Gerçek ikinci fotoğraf verisi yok |
| 6 | **[Faz 1]** Aynı desen farklı renk aynı ürün mü sayılmalı? (production için) | MVP'de geçici olarak "aynı ürün" kabul edildi (karar #25); production için kesin değil — benchmark bu konuyu kanıtlamadı |
| ~~7~~ | ~~**[Faz 4]** Log formatı ve logging altyapısı~~ | **Çözüldü** — bkz. karar #50 (FAZ 4C, 2026-09-01) |
| ~~8~~ | ~~**[Faz 1]** "Minimum benzerlik (%)" alanı için varsayılan değer~~ | **Çözüldü** — kullanıcı 80 değerini ve boş girdide de 80 kullanılmasını onayladı (2026-09-04, karar #76). Kaynak commit/publish durumu için güncel devir notuna bakın. |
| 9 | **[ClickOnce]** Gerçek yayıncı/şirket adı (`PublisherName`) | Şu an `Properties/PublishProfiles/ClickOnce.pubxml`'de açık placeholder (`[PLACEHOLDER] Yayıncı Adı Belirlenmedi`) — bkz. karar #84, `docs/CLICKONCE.md` §0 |
| 10 | **[ClickOnce]** Gerçek UNC (`\\sunucu\...`) veya HTTPS güncelleme/kurulum adresi | `InstallUrl`/`UpdateUrl` şu an bilerek boş — adres netleşmeden otomatik güncelleme kontrolü işlevsiz kalır. Bkz. karar #84, `docs/CLICKONCE.md` §0/§5 |
| 11 | **[ClickOnce]** Şirket kod imzalama sertifikası | Şu an yok, `SignManifests=false` — manifestler imzasız, Windows "Bilinmeyen yayıncı" uyarısı gösterebilir. Bkz. karar #84, `docs/CLICKONCE.md` §0/§7 |

Not: Arayüz teknolojisi (#15/#21), embedding depolama biçimi (#23), local
cache konumu/atomic write/config ayrımı (#39-45) artık **Confirmed**.
"Override restart sonrası hatırlanmalı mı" (#43 ile — hatırlanmaz, session-
only, explicit "varsayılan yap" hariç) ve "ayarlar dosyası şeması" (#41-42
ile) netleşti; tablodan kapatıldı.

---

## Later Phase (Şimdilik Karar Gerektirmeyen)

- Text search mimarisi (MVP'de yok — karar #19)
- Login / kullanıcı yönetimi (production'da planlı, MVP'de yok — karar #16)
- Raporlama
- Çoklu kullanıcı ölçeklenmesi
- Vector database / ANN index (ölçek ~5000'den çok büyürse yeniden
  değerlendirilecek — bkz. karar #32, `docs/PRODUCTION_REQUIREMENTS.md` §5)
- Dış AI servisi kullanımı (yönetim onayı verirse)
- Recursive alt klasör taraması (şu an gerekli değil — karar #32)

**Kalıcı olarak kapsam dışı (Later değil, artık hiç planlanmıyor):**
- Fabrika DB entegrasyonu — görsel kaynağı olarak (karar #31, SUPERSEDES #17)
- Ürün ekleme / CRUD ekranları, yeni ürün girişi Lens üzerinden (karar #30, SUPERSEDES #18)

---

## Karar Alma Süreci

Yukarıdaki "Not Yet Decided" maddeleri, kullanıcı ve/veya Tech Lead/CTO ile
yapılacak mimari toplantı sonrası bu tabloya "Confirmed" olarak taşınacaktır.
Onay olmadan hiçbir madde implementasyona esas alınmaz (bkz. `CLAUDE.md`).
